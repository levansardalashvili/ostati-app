# ostati-app — პროექტის კონტექსტი

**ეს ფაილი ცოცხალი დოკუმენტია.** ყოველ ჯერზე, როცა ერთად ვიღებთ ახალ არსებით არქიტექტურულ ან პროდუქტულ გადაწყვეტილებას (ახალი Firestore collection/ველი, ახალი auth ნაკადი, დაფიქსირებული პროდუქტული წესი, ბიბლიოთეკის შეცვლა), საჭიროა კითხვა: **"განვაახლო თუ არა CLAUDE.md?"** — არ განახლდეს დუმილში და არც წვრილმან ცვლილებებზე.

## პროდუქტის არსი

მობილური marketplace (iOS + Android), რომელიც აკავშირებს **მომხმარებლებს** (სახლის სერვისის საჭიროება — სანტექნიკოსი, ელექტრიკოსი და ა.შ.) **ადგილობრივ ოსტატებთან** საქართველოში. ორი როლი: **Customer** და **Provider**, ერთ ანგარიშს ერთი როლი აქვს (role-switch მექანიზმი არ არსებობს). მთელი კომუნიკაცია აპშივეა (ჩატი), არა WhatsApp/SMS-ზე გადასვლით.

მუშავდება მარტო, საშუალო React Native გამოცდილებით. მიზანია App Store + Play Store-ზე გასვლა — ამიტომ არქიტექტურა თავიდანვე სტაბილურია, არა disposable prototype.

## წყაროები

- **`docs/design-reference/product-spec.md`** — სრული პროდუქტის სპეციფიკაცია (ეკრანები, ლოგიკა, წესები)
- **`docs/design-reference/app-states.md`** — loading/empty/error/success state-ების სტანდარტი
- **`docs/design-reference/notifications-feature.md`** — შეტყობინებების სისტემის სპეციფიკაცია
- **`docs/design-reference/theme-reference.css`** — ორიგინალი ფერების პალიტრა (Figma Make-ის web პროტოტიპიდან)
- ორიგინალი დიზაინის რეფერენსი (Figma Make-ის React web კოდი, `App.tsx`) — ვიზუალური/ინტერაქციული სიზუსტის წყარო ეკრანების აგებისას. **პრიორიტეტის წესი:** კონფლიქტისას დიზაინის რეფერენსი იმარჯვებს `product-spec.md`-ზე, გარდა სამი დაფიქსირებული წესისა (იხ. ქვემოთ).

## ტექნიკური სტეკი (საბოლოო გადაწყვეტილება)

- **Expo (React Native) + TypeScript**, `blank-typescript` ტემპლეიტი (არა Expo Router — React Navigation)
- **React Navigation** — `@react-navigation/native-stack` (root) + `@react-navigation/bottom-tabs` (Customer/Provider Home-ის ირგვლივ, იხ. "ნავიგაციის არქიტექტურა")
- **Supabase** (`@supabase/supabase-js`, JS/REST-ზე დაფუძნებული, native მოდულების გარეშე) — Auth + Postgres ბაზა. **Firebase-ის ნაცვლად, #52-ის მიხედვით** (თავდაპირველად Firebase იყო არჩეული, მაგრამ Firebase Auth-ის ახალმა reCAPTCHA Enterprise-ზე დაფუძნებულმა abuse-დაცვამ ვერასოდეს გაატანა registration/sign-in ამ პროექტში — იხ. #52 სრული მიზეზისთვის).
- **`@supabase/supabase-js`** სესიის შესანახად სჭირდება **`@react-native-async-storage/async-storage`** (persist/refresh token) და **`react-native-url-polyfill`** (`index.ts`-ის თავში `import 'react-native-url-polyfill/auto'`)
- **`@react-native-google-signin/google-signin`** — Google Sign-In (native ID token მოაქვს, Supabase-ს `signInWithIdToken`-ით გადაეცემა)
- **EAS Build + dev client** — Expo Go არ გამოიყენება (native მოდულების გამო)
- **`lucide-react-native`** (+ `react-native-svg` peer dep) — აიქონები; დიზაინის რეფერენსი `lucide-react`-ს იყენებდა, ეს ზუსტი RN შესატყვისია
- **`expo-linear-gradient`** — გრადიენტული ბარათებისთვის (CTA ბარათები, სტატისტიკის ბარათი)
- **`expo-image-picker`** — კამერა/გალერეიდან ფოტოს არჩევა (#61, Storage-ის რეალურ ატვირთვასთან ერთად დამატებული, native module)
- **`metro.config.js`**-ში დამატებულია `mjs` sourceExt — `lucide-react-native`-ის ESM icon barrel-ის Metro-ს resolve-ისთვის საჭირო

## Auth მეთოდი (Supabase)

**მხოლოდ Email/Password + Google Sign-In.** არა ტელეფონის ნომერი, არა SMS/OTP ვერიფიკაცია — ეს წესი აშკარად ამორიცხულია დიზაინის რეფერენსიდან (`create-account-form.md`).

- რეგისტრაცია: ელფოსტა, **მისამართი — მხოლოდ Customer-ისთვის** (დიზაინის რეფერენსის დამატება `product-spec.md`-ის საწყის აღწერაზე, პრიორიტეტის წესით; Provider-ისთვის მისამართი მოთხოვნიდან მთლიანად ამოღებულია — იხ. ქვემოთ), პაროლი+დადასტურება. **სახელი/გვარი ორივე როლისთვის ორ ცალკე ველად** (`სახელი`, `გვარი`) — მომხმარებლის აშკარა მოთხოვნით override-ავს ზიპის ერთიან "სახელი და გვარი" ველს (თავდაპირველად მხოლოდ Customer-ისთვის იყო გაყოფილი, მოგვიანებით Provider-ზეც გავრცელდა იმავე მოთხოვნით).
- **Provider-ის რეგისტრაციაში მისამართის ველი და მისი ვალიდაცია საერთოდ არ ჩანს** (არც `RegisterScreen`-ზე, არც `GoogleCompleteScreen`-ზე) — მომხმარებლის აშკარა მოთხოვნით, Provider-ს საცხოვრებელი მისამართი საერთოდ არ სჭირდება, სამუშაო არეალს (`RegionAreaPicker`) მოგვიანებით, `ProviderSetupScreen`-ზე ირჩევს. `GoogleCompleteScreen`-ზე Provider-ისთვის submit ღილაკი დაუყოვნებლივ აქტიურდება (Google-ის ბარათის confirm-ის მეტი აღარაფერი სჭირდება ამ ეკრანზე). Customer-ისთვის მისამართი უცვლელად სავალდებულოა (ორივე ეკრანზე).
- Google რეგისტრაცია: სახელი/ელფოსტა/ფოტო წამოღებულია Google-იდან, Customer მხოლოდ აკლებს მისამართს (`GoogleCompleteScreen`) — Provider-ს დამატებითი ველი აღარ სჭირდება. Customer-ისთვის mock Google-ის სახელი იყოფა firstName/lastName-ად პირველი space-ით (Provider-ისთვისაც იგივე split გამოიყენება, #44-ის `ProviderProfileContext`-ში ჩასაწერად).
- როლი (Customer/Provider) აირჩევა registration-მდე (`RoleSelectScreen`) და გადაეცემა navigation param-ად Register/GoogleComplete ეკრანებზე — **Login ეკრანს როლი არ სჭირდება** (რეალურ ანგარიშში როლი უკვე განსაზღვრულია, `users` ცხრილიდან უნდა წამოვიდეს ავტორიზაციის შემდეგ)
- **Auth რეალურად დაკავშირებულია Supabase-თან (#52)** — თავდაპირველად Firebase იყო (#50-#51), მერე გადავინაცვლეთ Supabase-ზე (#52-ის მიზეზისთვის). ეტაპობრივად ("ნელნელა", მომხმარებლის მოთხოვნით) დაემატა: `users`/`provider_profiles`/`job_posts`/`job_responses`/`messages`/`reviews` ცხრილები (#51-#58), ჩატის Realtime (#59), საჯარო Provider დირექტორია (#60), Storage-ის ეტაპი A (#61).

## Supabase ცხრილები (დაგეგმილი სქემა, ჯერ არ არის იმპლემენტირებული)

`product-spec.md`-ის ტექნიკური მონიშვნიდან, Firestore collection-ების მაგივრად Postgres ცხრილებად (#52):

- `users` — **implementiert** (#51-ში აშენდა Firestore-ზე, #52-ში გადატანილია Supabase-ზე უცვლელი ველებით)
- `provider_profiles` — **implementiert** (#53; #60-ში დაემატა `first_name`/`last_name` დუბლირებულადაც, საჯარო დირექტორიისთვის) + საჯარო read-policy (#60)
- `job_posts` — **implementiert** (#54 "ეტაპი A" + #55 "ეტაპი B" — Customer-ის მხარეც, Provider-ის Job Feed-იც; #61-ში დაემატა `photos` სვეტი, #63-ში Customer-ის მხარეს ჩვენებაც დაემატა; `job_responses`/დანიშვნა/სტატუსების ცვლილება ჯერ არ არის)
- `job_responses` — **implementiert** (#56 — write-მხარე სამივე ეკრანზე რეალურია, Customer-ის read-მხარე ნაწილობრივ, #56-ის შენიშვნით)
- `conversations` — არ არსებობს, განზრახ (#57-ის შენიშვნა — საუბრის იდენტობა `messages`-ის (customer_id, provider_id) წყვილითვეა მოდელირებული)
- `messages` — **implementiert** (#57 ტექსტი + #59 Realtime + #66 structured "offer" ტიპი)
- `reviews` — **ნაწილობრივ implementiert** (#58 — წერა რეალურია, წაკითხვა მხოლოდ `ProviderReviewsScreen`-ზე; `ViewProviderProfileScreen` განზრახ mock-ზეა, საჯარო Provider დირექტორიის მოლოდინში)

საბოლოო ველების სტრუქტურა (და RLS policy-ები) კოდში ჩამოყალიბდება, როცა თითოეულ ცხრილს ვუკავშირებთ.

## მთავარი არქიტექტურული/პროდუქტული გადაწყვეტილებები

1. **კატეგორიის ფილტრი Browse-ზე — მრავალარჩევანი (multi-select).** დაფიქსირებული წესი `product-spec.md`-დან, ზედმეტდება დიზაინის რეფერენსის (single-select) ვერსიაზე. Implementiert `CustomerHomeScreen`-ში `Set<string>` state-ით.
2. **ფასის შეთანხმება ჩატში — სტრუქტურირებული ბარათი**, არა თავისუფალი ტექსტი. Provider აგზავნის რიცხვს ცალკე card-ის სახით (`ChatConversationScreen`-ის კომპოზერში ცალკე Wallet ღილაკით, provider-ისთვის მხოლოდ), Customer ეთანხმება/უარყოფს პირდაპირ ბარათიდან (`დათანხმება`/`უარყოფა` ღილაკები, ჩანს მხოლოდ `role==='customer' && !isMe && offerStatus==='pending'`-ზე). **D3 Implementiert** — ზიპში რეფერენსი არ არსებობდა, დიზაინი თავიდან შემუშავდა (`ChatMsg`-ის ახალი `type: 'offer'`, `src/data/mockChats.ts`). დათანხმებული ფასის შენახვა job-ის ჩანაწერში (Firestore) **ჯერ არ არის დაკავშირებული** — ამჟამად მხოლოდ ჩატის ლოკალურ state-ში ცვლის სტატუსს (`TODO: Firestore`-ის კომენტარი `respondToOffer`-ში).
3. **ფოტოს ატვირთვის ლიმიტი job post-ში — 1-3 ფოტო**, არა 5 (დიზაინის რეფერენსზე override).
4. **ერთ ანგარიშს ერთი როლი.** არა role-switch UI.
5. **ერთი აქცენტის ფერი მთელ აპში** (`#2563EB`) — დიზაინის რეფერენსში RoleSelect-ის ბარათებს ჰქონდა ლურჯი/იისფერი split, მაგრამ ეს override-ავს `product-spec.md`-ის ზოგადი დიზაინის წესით (`src/theme/colors.ts`).
6. **Job-ის სტატუსების ციკლი:** მომლოდინე → დადასტურებულია → დასრულების დადასტურების მოთხოვნა → დახურული + შეფასება. **Implementiert** — `CustomerJobDetailScreen`-ს აქვს სრული დასრულების/პრობლემის/შეფასების flow (ამბერ card "სამუშაო დასრულდა?" → celebration BottomSheet → `RatingScreen`), ზუსტად ზიპის App.tsx-ის CustomerJobDetail-ის მიხედვით. `ProviderJobDetailScreen`-ს დაემატა `completed` mode (მიღებული შეფასების ჩვენებით).
7. **ტელეფონის ნომერი/საკონტაქტო ინფო არასდროს ჩანს ავტომატურად** — მხოლოდ ორმხრივი გადაწყვეტილებით ჩატში.
8. **Provider-ის სპეციალობის არჩევა — dropdown + "სხვა", მრავალარჩევანი.** თავდაპირველად ერთი-არჩევანით შემოვიდა, მოგვიანებით მომხმარებლის მოთხოვნით მრავალარჩევანზე შეიცვალა (Provider-ს შეუძლია რამდენიმე სპეციალობის ერთდროულად მონიშვნა). გაზიარებული კომპონენტი `src/components/SpecialtyPickerField.tsx` (ველი + BottomSheet checkbox სია + "სხვა"-ს თავისუფალი ტექსტი, შეიძლება რამდენიმე custom ჩანაწერის დამატებაც, თითოეული ცალკე წაშლადი chip-ით; "დასრულება" ღილაკი ხურავს sheet-ს) — გამოიყენება `ProviderSetupScreen`-სა და `ProviderEditProfileScreen`-ში ზუსტად იგივე ლოგიკით (`value: SpecialtyOption[]`).
   - **გამოცდილების ველი** — თავისუფალი რიცხვის სტეპერის ნაცვლად dropdown fixed-დიაპაზონებით (`1 წელზე ნაკლები / 1–2 / 3–5 / 6–10 / 10+ წელი`), `src/data/experience.ts` + `src/components/ExperiencePickerField.tsx`, იგივე ორ ეკრანზე.
   - **"დაფარვის რაიონები" → "სამუშაო არეალი"** — მომხმარებლისთვის ლეიბლი უფრო გასაგები გახდა (ProviderProfile-ის მენიუ, ProviderServiceAreas/RegionAreaPicker-ის სათაურები, ViewProviderProfile-ის სექცია, ProviderSetup-ის ველის ლეიბლი). ფუნქციონალურად უცვლელი — `RegionAreaAccordion` კომპონენტი კვლავ საერთოა რეგისტრაციასა (`RegionAreaPickerScreen`) და პროფილის (`ProviderServiceAreasScreen`) რედაქტირებას შორის.
15. **"ფასი კვ.მ-ზე" — მხოლოდ იმ სპეციალობებზე, სადაც კვადრატულობით ითვლება ფასი.** `src/data/specialties.ts`-ში თითოეულ სპეციალობას აქვს `pricePerSqm: boolean` (true: მღებავი, კაფელი/მეტლახი, იატაკი/ლამინატი/პარკეტი, გიფსოკარდონი/შიდა რემონტი — ეს ბოლო სამი ახალი სპეციალობაა, დაემატა სპეციალურად ამ ფუნქციისთვის, SPECIALTIES 5-დან 8 ჩანაწერამდე გაფართოვდა; false: სანტექნიკოსი, ელექტრიკოსი, ავეჯი, კონდიციონერი). `SqmPriceField` კომპონენტი (`src/components/SqmPriceField.tsx`) რენდერდება ცალ-ცალკე **თითო შერჩეულ სპეციალობაზე** (`Record<specialtyId, priceString>` state, არა ერთი საერთო მნიშვნელობა) — Provider-ის მრავალარჩევანიანი სპეციალობის (გადაწყვეტილება #8) პირდაპირი შედეგია. მოთხოვნადი "სხვა" (custom) სპეციალობებზე ეს ველი არასდროს ჩანს, რადგან `pricePerSqm` მხოლოდ წინასწარ განსაზღვრულ `SPECIALTIES`-ზეა ცნობილი. გამოიყენება `ProviderSetupScreen`-სა და `ProviderEditProfileScreen`-ში სპეციალობის ველის ქვემოთ. **საჯარო პროფილზე (`ViewProviderProfileScreen`) ჯერ არ ჩანს** — მხოლოდ input-side აშენდა ამ ეტაპზე, საჯარო ჩვენება არ იყო მოთხოვნილი.
9. **Provider-ის სამუშაო რაიონები — მთელი საქართველოს მხარეები**, არა მხოლოდ თბილისის რაიონები. `src/data/georgiaRegions.ts` (11 მხარე, თბილისის რაიონები `districts.ts`-იდან საერთო წყაროა), გაზიარებული accordion კომპონენტი `src/components/RegionAreaAccordion.tsx` (მხარე იშლება, პირველი პუნქტი "ყველას მონიშვნა", მერე ცალკეული რაიონები) — გამოიყენება რეგისტრაციის `RegionAreaPickerScreen`-შიც (callback-ის საშუალებით, ცალკე root-stack ეკრანი) და პროფილის `ProviderServiceAreasScreen`-შიც (პირდაპირ ჩაშენებული).
10. **სერვისის კატეგორიები — 15 კატეგორია** (`src/data/categories.ts`), მომხმარებლის მიწოდებული სრული სია. Customer Home-ის "სერვისები" სექცია აღარ არის სქროლადი chip-ების რიგი — ახლა 2×2 ბადეა: **3 ყველაზე მოთხოვნადი** (`TOP_CATEGORY_IDS` — სანტექნიკა/ელექტროობა/დასუფთავება) + **"ყველა სერვისი"** ღილაკი. ეს ღილაკი ხსნის `CustomerCategoriesScreen`-ს (ყველა 15 კატეგორიის ბადე), საიდანაც კონკრეტულ კატეგორიაზე დაჭერით იხსნება `CustomerCategoryScreen` (მხოლოდ იმ კატეგორიის ოსტატები). Top-3 ღილაკებზე დაჭერა კვლავ inline მრავალარჩევანიან ფილტრს ამატებს Home-ის სიაში (გადაწყვეტილება #1 დაცულია).
11. **`CustomerCategory`-ის ორიგინალი ზიპის ვერსია იყო ბაგიანი** — `PROVIDERS.map()`-ს იყენებდა `.filter(category)`-ის გარეშე (ყველა ოსტატს უჩვენებდა, კატეგორიის მიუხედავად) და თავად ზიპშივე არსად არ იყო რეალურად გამოძახებული (orphan screen). ჩვენთან გასწორებულია (რეალურად filter-ავს) და დაკავშირებულია "ყველა სერვისი" ნაკადში.
12. **Global state პირველად ჩნდება: `CustomerProfileContext`** (`src/state/CustomerProfileContext.tsx`, App.tsx-ში `RootNavigator`-ის გარშემო). ინახავს `firstName/lastName/email/defaultAddress`-ს — რეგისტრაცია (ორივე გზა) წერს, `CustomerHome`/`CustomerProfile`/`CustomerEditProfile` კითხულობენ/ცვლიან, `PostJobScreen`-ის მისამართის ველი მხოლოდ **წინასწარ ივსება** `defaultAddress`-ით mount-ზე და **არასდროს არ წერს უკან** — job-ისთვის შეცვლილი მისამართი (მაგ. მეგობრის სახლისთვის) პროფილის default-ს არ ეხება. Provider-ისთვის ანალოგიური context ჯერ არ არსებობს — Provider-ის სახელი/სპეციალობა ყველგან ჯერ კიდევ hardcoded/ლოკალური state-ია (`ProviderProfileScreen`, `ProviderSetupScreen`, `ProviderEditProfileScreen`).
13. **`ProviderSetupScreen`-ს "გამოტოვება" აღარ აქვს — პროფილის შევსება სავალდებულოა.** მომხმარებლის აშკარა მოთხოვნით ამოღებულია `handleSkip`/header-ის "გამოტ." ღილაკი; გაგრძელება მხოლოდ `canSave`-ის (სპეციალობა + სამუშაო რაიონი) დაკმაყოფილებით შესაძლებელია.
14. **Provider-ის სერთიფიკატები და ნამუშევრების ფოტოები — ორივე არასავალდებულო, მრავალარჩევანი (ერთი ან რამდენიმე), preview/დამატება/წაშლა.** გაზიარებული კომპონენტები: `src/components/MediaUploadGrid.tsx` (ბადე დამატება-ღილაკებით, PostJobScreen-ის ფოტოს ატვირთვის იგივე ვიზუალური ენით, ლიმიტის გარეშე) და `src/components/MediaPreviewModal.tsx` (გადიდებული preview, სურვილისამებრ "წაშლა"). გამოიყენება `ProviderSetupScreen`-სა და `ProviderEditProfileScreen`-ში (ორივეგან ლოკალური state, არა Firestore-თან დაკავშირებული — `// TODO: Firestore`). `ViewProviderProfileScreen`-ზე (Provider-ის საჯარო პროფილი) ჩანს ორივე სექცია, მხოლოდ როცა სია არ არის ცარიელი — მონაცემი mock `PROVIDERS`-ის ახალი `certificates`/`portfolio` ველებიდან მოდის (`src/data/mockHomeData.ts`), ჯერჯერობით მხოლოდ `p1`-ს აქვს შევსებული მაგალითები.
16. **Provider-ის ფასის შეთავაზება "დაინტერესებისას".** `ProviderJobDetailScreen`-ში "დაინტერესება"-ზე დაჭერა აღარ აყენებს `expressed`-ს პირდაპირ — ჯერ იხსნება BottomSheet არასავალდებულო ფასის ველით (label "შეთავაზებული ფასი", placeholder **"ჩაწერეთ თანხა"**, არასდროს `0`), "დაინტერესების გაგზავნა" ადასტურებს. შეთავაზებული ფასი ჩანს დაინტერესების ღილაკის ტექსტში (`შეთავაზდა: N ₾`). **Customer-ის მხარეს ჯერ არ არის რეალურად დაკავშირებული** (Provider/Customer job-ის feed-ები ცალკე mock მონაცემებია — იხ. #17-ის შენიშვნა) — `// TODO: Firestore` `jobResponses`-ში.
17. **Customer-ის "დაინტერესებული ოსტატის" ბარათზე ფასის ჩვენება, 3 დონის fallback-ით:** 1) ამ job-ზე ხელით შეთავაზებული ფასი (`INTERESTED_PROVIDERS`-ის ახალი შეიცვლილი ტიპი — `{provider: Provider; offeredPrice?: string}[]`, ნაცვლად უბრალო `Provider[]`-ისა), 2) წინააღმდეგ შემთხვევაში `Provider.sqmPrice` (თუ ოსტატის სპეციალობა კვ.მ-ზეა ფასიანი — momentarily მხოლოდ `p2`-ს (მღებავს) აქვს mock-ში, #15-ის sqm-ღირებულების ცნებასთან კავშირშია, თუმცა ეს ორი მონაცემი ცალკე ცხოვრობს: `Provider.sqmPrice` არის საჯარო mock ველი, `sqmPrices`-ის state კი ProviderSetup/EditProfile-ის ლოკალურია — ჯერ არ არის დაკავშირებული ერთმანეთთან), 3) თუ არცერთი — `ფასი სამუშაოს ნახვის შემდეგ განისაზღვრება`. ასევე დაემატა **დალაგების chip-ები** ("ფასით"/"რეიტინგით"/"გამოცდილებით") `CustomerJobDetailScreen`-ში, ჩანს მხოლოდ 2+ დაინტერესებულ ოსტატზე.
18. **Customer-ის შეფასება job-ის დასრულების შემდეგ — სავალდებულო, "მოგვიანებით" არ არსებობს.** `CustomerJobDetailScreen`-ის completion BottomSheet-ს `onClose` აღარ ხურავს sheet-ს (no-op) — ერთადერთი გზა წინ არის "ოსტატის შეფასება" ღილაკი, რომელიც `RatingScreen`-ზე გადადის. `RatingScreen`-ს `gestureEnabled: false` აქვს დაყენებული და `navigation.addListener('beforeRemove', ...)`-ით ბლოკავს ყველანაირ უკან დაბრუნებას (header-ის ისარი `showBack={false}`-ით მოხსნილია, hardware back, swipe gesture) მანამ, სანამ შეფასება არ გაიგზავნება (`submitted === false`). ვარსკვლავის მინიმუმ 1 არჩევანი უკვე ადრეც სავალდებულო იყო ღილაკის `disabled={stars === 0}`-ით — ეს უცვლელია. ტექსტური review კვლავ არასავალდებულოა. `BackHeader`-ს დაემატა გაზიარებული `showBack?: boolean` პროპი ამ მიზნით (default `true`, უკვე არსებულ ეკრანებზე ქცევა უცვლელია).
19. **RatingScreen-ზე Customer-ს შეუძლია დასრულებული სამუშაოს ფოტოს ატვირთვა** (არასავალდებულო, გადაღება/გალერეა — იგივე `MediaUploadGrid`/`MediaPreviewModal` კომპონენტები, რაც Provider-ის სერთიფიკატებზე/portfolio-ზე, #14). ატვირთული ფოტოები `RatingData.photos?`-ში ჩნდება (`src/data/mockHomeData.ts`) და გამოსახულია `CustomerJobDetailScreen`-ის "შენი შეფასება" სექციაში. **შეფასების გაგზავნის შემდეგ Customer პირდაპირ Customer Home-ზე ბრუნდება** (`navigation.reset`) — აღარ `navigation.goBack()`-ით CustomerJobDetailScreen-ზე; submitted-ეკრანის ღილაკი გადარქმეულია "მთავარზე დაბრუნება"-დ.
20. **Customer Home-ის რაიონის ფილტრი ოსტატის სამუშაო არეალის (`Provider.areas`) მიხედვით ფილტრავს**, არა მისი საცხოვრებელი მისამართის (`location`) — ეს ცვლილება override-ავს წინა ქცევას. დაემატა **"ჩემი რაიონი"** ჩიპი ("ყველა"-ს შემდეგ, დანარჩენ რაიონებამდე) — `selDistrict`-ს აქვს `'mine'` სენტინელ მნიშვნელობა, `myDistrict`-ად resolve-ავს Customer-ის `defaultAddress`-ს `TBILISI_AREAS`-თან substring-შედარებით (address ფორმატი აპში არაერთგვაროვანია — ხან "რაიონი, ქალაქი", ხან პირიქით — substring-ით ორივეს იჭერს); თუ resolve ვერ ხერხდება, ჩიპი საერთოდ არ ჩანს (არა disabled მდგომარეობა).
21. **"ხელმისაწვდომი ოსტატები" → "ტოპ ოსტატები შენს რაიონში"**, რანჟირებული (არა ჩამონათვალის/რეგისტრაციის თანმიმდევრობით). `providerRankScore()` (`CustomerHomeScreen.tsx`) — Bayesian/წონიანი საშუალო ფორმულა (IMDB-ის პრინციპი, `RATING_PRIOR_COUNT=15`, `RATING_PRIOR_MEAN=4.3`), რომელიც არ აძლევს უპირატესობას მცირერიცხოვან შეფასებას ასობით კარგზე მეტად — რაც მეტი `reviews`, მით უფრო ენდობა რეალურ `rating`-ს baseline-ის ნაცვლად. პლუს მცირე log-სკალირებული წონა `jobs`-ზე (დასრულებული სამუშაოები) და მინიმალური tie-breaker `online`-ზე (ბოლო აქტივობის პროქსი — mock მონაცემებს ნამდვილი "ბოლო აქტივობის დრო" არ აქვს).
22. **Customer-ის Bottom Tabs 3-დან 4 ჩანართამდე გაფართოვდა: Home / "მოთხოვნა" / ჩატები / პროფილი** — `product-spec.md`-ის საწყისი "3 ჩანართის" წესის override მომხმარებლის მოთხოვნით ("მოთხოვნის გამოქვეყნება" წინა Home-ის ჩაშენებული gradient CTA ბარათიდან ამოღებულია მთლიანად და ამ ტაბში გადმოვიდა, უფრო ხელმისაწვდომი ადგილისთვის). "მოთხოვნა" ტაბს საკუთარი ეკრანი/კონტენტი არ აქვს — `PostJobTabPlaceholder` არასდროს ნამდვილად არ ჩანს, `tabPress` listener ყოველთვის `e.preventDefault()`-ს იძახებს და `navigation.getParent()?.navigate('PostJob')`-ით root-stack-ის `PostJob`-ზე გადადის (`CustomerTabs.tsx`). Provider-ის ტაბებზე ეს არ ვრცელდება (`ProviderTabs` კვლავ 3 ჩანართია).
23. **Post Job ფორმის გამარტივება:** კატეგორია აღარ არის მუდმივად expanded სია — ველი-ღილაკი + BottomSheet, ერთი არჩევანი (`SPECIALTIES`-დან). "რა სამუშაო გჭირდება?" (title) ველი მთლიანად ამოღებულია — რჩება მხოლოდ "სამუშაოს აღწერა". "როდის გჭირდება?" (urgency) სექცია მთლიანად ამოღებულია. "სასურველი თარიღი" ახლა `DatePickerField`-ია (`src/components/DatePickerField.tsx` + `src/components/CalendarPicker.tsx` — pure RN თვის ბადის კალენდარი, native date-picker ბიბლიოთეკის დამატების გარეშე, რადგან native module-ს dev client-ის ხელახლა აწყობა დასჭირდებოდა); თარიღის არჩევის შემდეგ ავტომატურად იხსნება "სასურველი დრო"-ს BottomSheet (მანამდე ეს ველი disabled-ია). დროის ბოლო ვარიანტი "დრო არ აქვს" → **"ნებისმიერ დროს"**.
24. **Customer Home-ზე "მიმდინარე სამუშაო" ბარათი** — ჩანს მხოლოდ მაშინ, როცა `CUSTOMER_JOBS`-ში არსებობს `status === 'active'` ჩანაწერი (ე.ი. Customer-მა უკვე აირჩია კონკრეტული Provider). ბარათში: კატეგორია (`CategoryIcon` + `CATEGORIES`-ის label), Provider-ის სახელი, თარიღი/დრო, `StatusPill`. დაჭერისას `navigation.navigate('CustomerJobDetail', {jobId})` — არსებულ ეკრანზე, ცალკე duplicate დეტალის ეკრანი არ აშენებულა.
25. **Provider-ის არჩევის შემდეგ job "იხურება" სხვა Provider-ებისთვის.** `CustomerJobDetailScreen`-ში `visibleInterestedList` — `selectedProvider`-ის არსებობისას `interestedList`-ს filter-ავს მხოლოდ არჩეულ ოსტატამდე (დანარჩენები აღარ ჩანან, როგორც აქტიური კანდიდატები); თუ ადრე 2+ ოსტატი იყო დაინტერესებული, ჩნდება შენიშვნა "მოთხოვნა დაიხურა სხვა ოსტატებისთვის". დალაგების chip-ები (#17) მხოლოდ არჩევამდე ჩანს (`!selectedProvider && interestedList.length > 1`).
26. **Provider Home-ის მისალმება გამარტივდა: "გამარჯობა, გიორგი"** (ერთსტრიქონიანი, მხოლოდ სახელი) — წინა ორსტრიქონიანი "დილა მშვიდობისა, / გიორგი ბერიძე" წესი override-ავს.
27. **Provider Home-ის ხელმისაწვდომობის subtitle გამარტივდა: "შეტყობინებები ჩართულია" / "შეტყობინებები გამორთულია"** (წინა "ახალი სამ. შეტყობინებები ჩართულია" / "პუშ შეტყობინებები გამორთულია"-ს ნაცვლად). Job Feed (`filtered` PROVIDER_FEED) `available`-ისგან დამოუკიდებელია და OFF-ის დროსაც ჩანს — ეს ისედაც ასე იყო კოდში (availability toggle არასდროს მალავდა feed-ს), მხოლოდ დადასტურდა და კომენტარით დაფიქსირდა.
28. **Availability-ის control მხოლოდ Provider Home-ზეა — შემოწმდა, დუბლირება არსად არ არსებობდა.** `ProfileSettingsScreen` (Provider-ის "ანგარიშის პარამეტრები") მხოლოდ პაროლის შეცვლას შეიცავს, `ProviderProfileScreen`-ზე "ხელმისაწვდომი" მხოლოდ სტატიკური ტექსტია (არა toggle) — არსად არ მოიძებნა ცალკე Availability control, ამიტომ კოდის ცვლილება ამ პუნქტში არ დასჭირდა.
29. **Provider Job Feed — "ახალი მოთხოვნები შენს სამუშაო არეალში" (რენეიმი), Home-ზე მხოლოდ ბოლო 5 ჩანს + "ყველას ნახვა".** ახალი root-stack ეკრანი `ProviderJobFeedScreen` (`src/screens/ProviderJobFeedScreen.tsx`) აჩვენებს სრულ (unlimited) ჩამონათვალს, იმავე ბარათით რაც Home-ზეა. კოდის დუბლირების თავიდან ასაცილებლად, Job Feed-ის ბარათი გატანილია გაზიარებულ კომპონენტში `src/components/ProviderFeedJobCard.tsx` (+ skeleton); ღია job-ების ლოგიკა — `src/data/providerFeedFilters.ts` (`getOpenProviderFeed()`). **კატეგორია/რაიონი/დრო ციკლური ფილტრ-chip-ები (3× "ყველა") მომხმარებლის მოთხოვნით მთლიანად ამოღებულია** — ორივე ეკრანზე (Home-ის ჩაშენებული ჩამონათვალი და სრული Feed screen) ახლა უბრალოდ ყველა ღია job ჩანს, დამატებითი ფილტრაციის გარეშე. წაშლილია: `src/components/FeedFilterChip.tsx` (ფაილი მთლიანად), `providerFeedFilters.ts`-ის `CAT_OPTS`/`CAT_IDS`/`AREA_OPTS`/`AREA_VALS`/`TIME_OPTS`/`TIME_VALS`/`filterProviderFeed()`.
30. **Provider Home-ზე "მიმდინარე სამუშაო" ბარათი + Job Feed-ის "დახურვის" ლოგიკა ერთი მექანიზმითაა გადაწყვეტილი.** `FeedJob`-ს დაემატა `assignedProviderId?: string | null` (`src/data/mockHomeData.ts`) — `PROVIDER_FEED`-ისვე ველი, არა ცალკე მონაცემთა კუნძული (განსხვავებით #16-17-ის Provider/Customer ცალკე feed-ებისგან, სპეციალურად ამიტომ, რომ "job Feed-იდან მიმდინარე სამუშაოში გადადის" ცნება იმავე ჩანაწერზე მუშაობდეს). `CURRENT_PROVIDER_ID = 'p1'` (`providerFeedFilters.ts`) წარმოადგენს "მე, ამჟამინდელ Provider-ს". `getOpenProviderFeed()` გამორიცხავს ნებისმიერ job-ს, სადაც `assignedProviderId` შევსებულია (განურჩევლად ვისზეც) — ასე ერთდროულად სრულდება ორივე წესი: "სხვამ აირჩია → Feed-იდან ქრება" და "მე ამირჩიეს → Feed-იდან ქრება (და "მიმდინარე სამუშაო"-დ ჩნდება)". Demo-სთვის `f2`-ს `assignedProviderId: 'p1'` (საკუთარი მიმდინარე job), `f4`-ს `assignedProviderId: 'p3'` (სხვამ აიღო, საერთოდ არ ჩანს). ბარათში: Customer-ის სახელი, კატეგორია, თარიღი/დრო, რაიონი, `StatusPill status="active"`; დაჭერისას `navigation.navigate('ProviderJobDetail', {id, mode: 'selected'})` — არსებულ ეკრანზე/რეჟიმზე, ცალკე დეტალის ეკრანი არ აშენებულა. `PROVIDER_MY_JOBS_ACTIVE` (`mockReviews.ts`, `ProviderMyJobsScreen`) ამ მექანიზმთან **არ არის დაკავშირებული** — ცალკე, adres-only mock (`{title, customer, addr, when, pay}`, `id`/`category` არც აქვს) რჩება უცვლელი, ცალკე კუნძულად.
31. **Provider Profile Edit-ს დაემატა "სახელი"/"გვარი" ცალკე ველები** (`ProviderEditProfileScreen`-ს ადრე ერთი "სახელი და გვარი" ჰქონდა) — შესაბამისობაშია რეგისტრაციის ორივე ველიან ფორმასთან. **სპეციალობის სელექტორი უკვე იყო იგივე მრავალარჩევანიანი dropdown, რაც რეგისტრაციაზე** (#8) — ცვლილება არ დასჭირდა, მხოლოდ დადასტურდა. **"სამუშაო არეალი" ველი ახლად დაემატა** ამ ეკრანზე (ადრე areas საერთოდ არ იყო `ProviderEditProfileScreen`-ში — მხოლოდ ცალკე `ProviderServiceAreasScreen`-ში იყო რედაქტირებადი) — იგივე `RegionAreaPicker`/`RegionAreaAccordion` მექანიზმით, რაც `ProviderSetupScreen`-ის რეგისტრაციისას (`navigation.navigate('RegionAreaPicker', {selected, onSave})`). `ProviderServiceAreasScreen`-ის ცალკე მენიუს ჩანაწერი უცვლელად რჩება (ორივე entry point თანაარსებობს, სინქრონიზებული არ არის ერთმანეთთან — ორივე ლოკალური state-ია, Firestore-მდე მოსალოდნელი).
32. **Provider Public Profile (`ViewProviderProfileScreen`) — ყველა მოთხოვნილი ველი უკვე იყო ან დაემატა.** რეიტინგი/შეფასებები/გამოცდილება/სამუშაო არეალი/სერთიფიკატები/ნამუშევრები **უკვე იყო** (სერთიფიკატები/ნამუშევრები სექციები უკვე იმალებოდა ცარიელობისას — `p.certificates.length > 0`/`p.portfolio.length > 0`, დამატებითი ცვლილება არ დასჭირდა). ახლად დაემატა: **"სპეციალობები"** სექცია (`Provider.specialties: string[]`, ახალი ველი mock-ში, დამოუკიდებელი `category`-სგან, რომელიც მხოლოდ ფილტრაციისთვის რჩება; `p1`-ს 2 სპეციალობა აქვს, დანარჩენებს — 1) და **"ფასი"** სექცია (თუ `p.price` — საათობრივი დიაპაზონი ჩანს, თუ `p.sqmPrice` — "ფასი კვ.მ-ზე: N ₾ / მ²", თუ არცერთი — "ფასი სამუშაოს ნახვის შემდეგ განისაზღვრება"; ორივე ერთდროულადაც შეიძლება გამოჩნდეს).
33. **ფასის 3 state-ის ერთგვაროვანი ტექსტი UI-ში.** `CustomerJobDetailScreen`-ის `priceLabel()` (#17) განახლდა — შეთავაზებული ფასი ახლა `"შეთავაზებული ფასი: N ₾"` ფორმატშია (ადრე უბრალოდ `"N ₾"` იყო, label-პრეფიქსის გარეშე). სამივე ფორმატი ერთგვაროვნადაა გამოყენებული აპში: `"შეთავაზებული ფასი: N ₾"` (Customer-ის job detail-ის დაინტერესებული ოსტატის ბარათზე), `"N ₾ / მ²"` (`SqmPriceField`, `ViewProviderProfileScreen`-ის "ფასი" სექცია, `priceLabel()`-ის sqm-fallback), `"ფასი სამუშაოს ნახვის შემდეგ განისაზღვრება"` (როცა არც ერთი ფასი არ არის მითითებული). Provider-ის საკუთარი "დაინტერესება" ღილაკის ტექსტი (`შეთავაზდა: N ₾`, #16) განზრახ დარჩა შემოკლებული — ღილაკის სივრცის შეზღუდვის გამო, არა Customer-ისთვის განკუთვნილი ინფორმაციული დისპლეი.
34. **Job-ის სტატუსების user-facing ტექსტი გასწორდა: "მომლოდინე" / "დადასტურებულია" / "დასრულებულია".** `StatusPill`-ის (`src/components/StatusPill.tsx`) `STATUS_MAP`-ში მხოლოდ `label` ველები შეიცვალა (`pending: 'მოლოდინი'→'მომლოდინე'`, `active: 'აქტიური'→'დადასტურებულია'`, `completed: 'დასრულდა'→'დასრულებულია'`) — შიდა `JobStatus` type-ის key-ები (`'active'|'pending'|'completed'|'cancelled'`) **განზრახ არ გადარქმეულა** (მასშტაბური რეფაქტორინგი იქნებოდა ბევრ ფაილში, მოთხოვნა კი მხოლოდ "user-facing" ტექსტზეა). სემანტიკა უცვლელი: `pending` = Provider ჯერ არ არჩეულა, `active` = Customer-მა Provider აირჩია ("Confirmed"), `completed` = Customer-მა დაადასტურა დასრულება და შეაფასა. `cancelled` ამ 3-საფეხურიანი ლისტის ნაწილი არაა (გამონაკლისი/terminal state) — ლეიბლი უცვლელია. თანმიმდევრულობისთვის იგივე რენეიმი გავრცელდა `CustomerJobsScreen`-ისა და `ProviderMyJobsScreen`-ის ტაბის ლეიბლებზეც (იმავე StatusPill ბარათებთან ერთად ჩანან).
35. **Customer-ს ფასის შეთავაზება/მითითება საერთოდ აღარ შეუძლია — ფასს მხოლოდ Provider ადგენს, job-ის აღწერის/ფოტოების ნახვის შემდეგ.** `PostJobScreen`-ის "სავარაუდო ბიუჯეტი" ველი მთლიანად ამოღებულია (state, input, ჰინტი); `budget` ველი მოცილებულია `CustomerJob`/`FeedJob` mock ტიპებიდანაც (`mockHomeData.ts`) და ყველა მათი mock ჩანაწერიდან — არა მხოლოდ ვიზუალურად დამალული. ყველა consumer გასწორდა: `CustomerJobDetailScreen` (ბიუჯეტის badge ამოღებულია, urgent badge-ს გამოცალკავებული `job.id === 'j1'` პირობით), `CustomerJobsScreen` (footer-ის budget/dot ტექსტი ამოღებულია), `ProviderFeedJobCard` (`სავ. ბიუჯ.` tag ამოღებულია), `ProviderJobDetailScreen` (`statsRow`-ს მხოლოდ "დაინტერესებული" სთეითი დარჩა, ცენტრირებული). PostJob-ის privacy-ბარათს დაემატა ახსნა: ფასს Provider ადგენს აღწერის/ფოტოს ნახვის შემდეგ და სთავაზობს ჩატში. **ეს არ ეხება Provider-ინიცირებულ ფასის ნაკადებს** — Provider-ის "დაინტერესებისას" შეთავაზებული ფასი (#16-17) და ჩატის სტრუქტურირებული ფასის შეთავაზება/დათანხმება/უარყოფა (#2) უცვლელი რჩება, რადგან ორივე უკვე Provider-ის მხრიდან წარმოშობილია, ზუსტად ამ მოთხოვნის შესაბამისად.
36. **Customer-ის მე-4 ტაბი ("მოთხოვნა") გახდა რეალური "ჩემი განცხადებები" ეკრანი, აღარ არის PostJob-ზე ცარიელი redirect.** ყოფილი root-stack "CustomerJobs" ეკრანი (Profile-ის "ჩემი მოთხოვნები" მენიუდან push-ილი) გახდა თავად ეს ტაბი — ცალკე ახალი ეკრანი არ აშენებულა, `CustomerJobsScreen`-ი უბრალოდ გადატანილია root-stack-იდან `CustomerTabParamList`-ში (`MyJobsTab`, ლეიბლი "განცხადებები" — მოკლე, ერთსიტყვიანი, სხვა ტაბების სტილში). `RootStackParamList`-დან `CustomerJobs` მთლიანად წაშლილია (აღარ არსებობს, დუბლირებული ეკრანის თავიდან ასაცილებლად); Profile-ის მენიუს "ჩემი მოთხოვნები" ახლა `navigation.navigate('MyJobsTab')`-ით ტაბზე გადადის push-ის მაგივრად. `CustomerJobsScreen`-ს BackHeader/back-ისარი მოეხსნა (ტაბის root-ს "უკან" აზრი არ აქვს) და ჩაენაცვლა მარტივი header-ით (სათაური "ჩემი განცხადებები" + წრიული "+" ღილაკი მარჯვნივ → `navigation.navigate('PostJob')`), მოთხოვნების ჩამონათვალი (active/pending/done ტაბები) უცვლელია.
37. **`CustomerEditProfileScreen`-ს "სახელი და გვარი" ერთი ველი გაეყო "სახელი"/"გვარი"-დ**, ზუსტად რეგისტრაციის (`RegisterScreen`) იგივე `nameRow` ლეიაუტით (ორი გვერდიგვერდ `flex: 1` ველი) — მომხმარებლის მოთხოვნით შესაბამისობაშია მოყვანილი. ცალ-ცალკე ველიდანვე იწერება `CustomerProfileContext`-ში (`setProfile({firstName, lastName, ...})`), აღარ არის საჭირო `name.split(' ')`-ის ევრისტიკა (რომელიც წინა ერთიან ველთან ერთად კარგად არ მუშაობდა მრავალსიტყვიან გვარებზე). ორივე ველს ცალკე სავალდებულობის ვალიდაცია აქვს (`firstNameErr`/`lastNameErr`), Save ღილაკი `canSave = !!firstName.trim() && !!lastName.trim()`-ზეა disabled. `ProviderEditProfileScreen`-ს ეს ველების გაყოფა უკვე ჰქონდა (#31) — ახლა ორივე Edit Profile ეკრანი თანმიმდევრულია რეგისტრაციის ფორმასთან.
38. **მისამართის ველი რეგისტრაციისას (`RegisterScreen`, `GoogleCompleteScreen` — customer-იც და provider-იც, ორივე ამ ორ საერთო ეკრანზე ავსებს მისამართს) ახლა რუკის შედეგების სიით ავსებადია, უბრალო თავისუფალი ტექსტის ნაცვლად.** მომხმარებელმა აირჩია: (1) **უფასო alternative** Google Maps/Places API-ს ნაცვლად (ფასიანი, API key + billing სჭირდება) — **OpenStreetMap Nominatim** (public, უფასო, API key არ სჭირდება); (2) **ტექსტი + შედეგების სია რუკის ვიზუალის გარეშე** (არა pin-drag რუკა) — ორივე გადაწყვეტილება მომხმარებელს დავუსვი პირდაპირ კითხვად და დაადასტურა. ახალი გაზიარებული კომპონენტი `src/components/AddressAutocompleteField.tsx`: მომხმარებელი წერს (მაგ. "ჭავჭავაძის 48"), 500ms დებაუნსით მოდის Nominatim-ის REST შედეგები (`countrycodes=ge`, `accept-language=ka`, ლიმიტი 5), ჩამონათვალი ჩნდება ველის ქვემოთ absolute-positioned dropdown-ად (არა `FlatList` — უბრალო `.map()`, რადგან ორივე ეკრანი `ScrollView`-შია/მის გვერდით და nested VirtualizedList-ის თავიდან ასაცილებლად), არჩევისას ველი ივსება არჩეული `display_name`-ით. თუ API-მ ვერაფერი იპოვა ან ხელმისაწვდომი არაა — თავისუფალი ტექსტის ჩაწერაც კვლავ მუშაობს უცვლელად (ვალიდაცია კვლავ უბრალოდ `value.trim()`-ზეა, არა არჩევის ფაქტზე) — Nominatim-ის public API-ს rate-limit აქვს (~1 req/sec), ამიტომ submit-ს არასდროს არ ვბლოკავთ მასზე დამოკიდებულებით. `ProviderSetupScreen`-ს ცალკე მისამართის ველი არ აქვს (Provider-იც იმავე `RegisterScreen`/`GoogleCompleteScreen`-ზე ავსებს მისამართს რეგისტრაციისას, სპეციალობის/არეალის ეტაპამდე) — ამიტომ ცალკე ცვლილება არ დასჭირდა. **მასშტაბის ზრდისას განსახილველია** საკუთარი Nominatim self-host ან ფასიანი Google Places (`CLAUDE.md`-ის შემდეგი განახლების საგანი, თუ საჭირო გახდება).
39. **`PostJobScreen`-ის "კატეგორია" BottomSheet ახლა იყენებს `CATEGORIES`-ს (`src/data/categories.ts`, 15 ცალკე კატეგორია), `SPECIALTIES`-ის (`src/data/specialties.ts`, 8 ჩანაწერი) ნაცვლად.** ბაგი იყო — `SPECIALTIES` განზრახული იყო მხოლოდ Provider-ის *საკუთარი პროფესიის* არჩევისთვის (`ProviderSetupScreen`/`ProviderEditProfileScreen`-ის `SpecialtyPickerField`, `pricePerSqm`-ის ლოგიკით, #15) და აერთიანებს რამდენიმე კატეგორიას ერთ პუნქტად (მაგ. "იატაკი / ლამინატი / პარკეტი" ერთი ჩანაწერია, სანამ `CATEGORIES`-ში "კაფელი / მეტლახი" და "ლამინატი / პარკეტი" ცალკეა) — ამის გამო Customer-ს job post-ის კატეგორია არ შეეძლო ისეთივე სიზუსტით აერჩია, როგორც დანარჩენ აპში (Customer Home, `CustomerCategoriesScreen`) ჩანს. ასევე `id`-ების სივრცეც განსხვავებული იყო (`SPECIALTIES`-ს `plumber`/`electrician`/... აქვს, `CATEGORIES`-ს — `plumbing`/`electrical`/...) — `mockHomeData.ts`-ის `FeedJob`/`CustomerJob`-ის `category` ველები `CATEGORIES`-ის id-ებს იყენებენ ყველგან (`CategoryIcon`-იც `CATEGORIES`-ზეა აგებული), ასე რომ წინა ვერსია ამ მონაცემთა მოდელთანაც შეუსაბამო იყო. BottomSheet-ის სია (15 პუნქტი, წინა 8-ის მაგივრად) `ScrollView`-ში გაეხვია `maxHeight: 420`-ით, რომ პატარა ეკრანზეც არ გადმოვიდეს ეკრანის ფარგლებს გარეთ.
40. **Provider-ის ტაბებიც 3-დან 4 ჩანართამდე გაფართოვდა: Home / "სამუშაოები" / ჩატები / პროფილი** — Customer-ის "MyJobsTab"-ის (#36) ზუსტად იგივე ლოგიკით, მომხმარებლის პირდაპირი მოთხოვნით ("ეგრე ჩავაშენოთ ოსტატისთვისაც, ოღონდ განცხადებები-ს მაგივრად იყოს ჩემი სამუშაო"). ყოფილი root-stack "ProviderMyJobs" ეკრანი (`ProviderMyJobsScreen`, ზიპის App.tsx-ის მიხედვით აშენებული, აქამდე ProviderHome-ის სტატისტიკის "სამ." უჯრიდან იხსნებოდა push-ით) გახდა თავად ეს ტაბი — ცალკე ახალი ეკრანი არ აშენებულა. `ProviderTabParamList`-ს დაემატა `MyJobsTab`, ტაბის ლეიბლი "სამუშაოები" (Customer-ის "განცხადებები"-ს იგივე მოკლე ერთსიტყვიანი სტილი, `ClipboardList` აიქონით). `RootStackParamList`-დან `ProviderMyJobs` მთლიანად წაშლილია. `ProviderMyJobsScreen`-ს `BackHeader`/back-ისარი მოეხსნა (ტაბის root-ს "უკან" აზრი არ აქვს) და ჩაენაცვლა მარტივი header-ით (მხოლოდ სათაური "ჩემი სამუშაოები", Customer-ის ვერსიისგან განსხვავებით "+" ღილაკის გარეშე — Provider სამუშაოს არ აქვეყნებს, მხოლოდ Job Feed-იდან ღებულობს). ორივე ძველი entry point ახლა ტაბზე გადადის push-ის ნაცვლად: `ProviderHomeScreen`-ის "სამ." სტატისტიკის უჯრა (`navigation.navigate('MyJobsTab')`) და **ახლად დამატებული** `ProviderProfileScreen`-ის "ჩემი სამუშაო" მენიუს პუნქტი (Customer-ის Profile-ზე ანალოგიური "ჩემი მოთხოვნები" პუნქტის არსებობის შესაბამისად — Provider-ის Profile-ს აქამდე ამ სკრინისკენ არანაირი ბმული არ ჰქონდა). ამასთანავე გასწორდა ლატენტური ტიპების ბაგი: `ProviderHomeScreen`-სა და `ProviderProfileScreen`-ს `Props`-ის კომპოზიტ ტიპი შეცდომით `CustomerTabParamList`-ს იყენებდა `ProviderTabParamList`-ის ნაცვლად (runtime-ზე უვნებელი იყო, რადგან ორივე ტაბის სიას იდენტური 'Home'/'Profile' key-ები ჰქონდა, მაგრამ არასწორი იმპორტი იყო) — ორივე ეკრანზე გასწორდა სწორ ტიპზე ამ ცვლილების ფარგლებში.
41. **Favorite/შენახული ოსტატები — ახალი ფუნქცია, ზიპში საერთოდ არ არსებობდა.** მომხმარებლის იდეით: Customer ❤️-ს აჭერს ოსტატის საჯარო პროფილზე (`ViewProviderProfileScreen`-ის header-ში, `Share2`-ის გვერდით — `favorite ? colors.destructive-ით შევსებული : outline`), Provider-ის საკუთარი პროფილის preview-ზე (`p.id === CURRENT_PROVIDER_ID`) ❤️ საერთოდ არ ჩანს (საკუთარი თავის შენახვა აზრს მოკლებულია). მეორე global state Customer-ისთვის `CustomerProfileContext`-ის (#12) შემდეგ — ახალი `src/state/FavoriteProvidersContext.tsx` (`Set<string>` favorite provider id-ების, `isFavorite`/`toggleFavorite`), `App.tsx`-ში `CustomerProfileProvider`-ის შიგნით ჩალაგებული. ახალი ეკრანი `src/screens/SavedProvidersScreen.tsx` (root-stack push, არა ტაბი — `PROVIDERS`-ს favorite id-ებით filter-ავს, თითო ბარათზე ❤️ პირდაპირ აქ იხსნება unfavorite-ისთვის, "მიწერა" ღილაკიც არის პირდაპირ ჩატში გადასასვლელად) — `CustomerProfileScreen`-ის მენიუში ახალი "შენახული ოსტატები" პუნქტი (`favoriteIds.size` badge-ით, 0-ზე ავტომატურად იმალება `ProfileMenuRow`-ის `!!badge`-ის წყალობით). TODO: Firestore-ში `users/{uid}/favoriteProviders` sub-collection-ად ჩანაცვლდება, როცა რეალურ auth-თან დაკავშირება მოხდება.
42. **"ახალი ოსტატი" state — 0 შეფასების Provider-ს არსად ეჩვენება "0.0 ★".** ახალი `src/utils/providerRank.ts`-ის `isNewProvider(p) => p.reviews === 0` ყველგან ცვლის ვარსკვლავიან რეიტინგს "ახალი ოსტატი" ბეჯით (secondary-ტინტიანი chip) — `CustomerHomeScreen`-ის ოსტატის ბარათი, `CustomerCategoryScreen`-ის სია, `ViewProviderProfileScreen`-ის სტატისტიკის ბარათი (`—` + "ახალი ოსტატი", რიცხვის მაგივრად), `SavedProvidersScreen`, `CustomerJobDetailScreen`-ის დაინტერესებული ოსტატის ბარათი. დემო-სთვის დაემატა მე-5 mock Provider `PROVIDERS`-ში (`p5`, `src/data/mockHomeData.ts` — `rating: 0, reviews: 0, jobs: 0`, კონდიციონერის სპეც., 1 წლის გამოცდილება) — აქამდე PROVIDERS-ის ყველა ჩანაწერს 70+ შეფასება ჰქონდა, ეს ახალი ბაზა ვერასდროს დატესტავდა ამ state-ს.
43. **Top Provider-ის რეიტინგი დაცულია მანიპულაციისგან — ერთმა 5.0/1 შეფასებამ ვერ გადააჭარბოს 4.9/180-ს ნებისმიერ დალაგებაში.** `CustomerHomeScreen`-ის "ტოპ ოსტატები" ალგორითმი (Bayesian/წონიანი საშუალო, #21) გატანილია `CustomerHomeScreen.tsx`-იდან საერთო `src/utils/providerRank.ts`-ში (`weightedRating`, `providerRankScore`) — ადრე ლოკალურად იყო განსაზღვრული ერთადერთ ეკრანზე, ამიტომ სხვაგან რეიტინგით დალაგება დაუცველი იყო. აღმოჩნდა და გასწორდა კონკრეტული დაუცველობა: `CustomerJobDetailScreen`-ის "რეიტინგით" sort chip (#17) პირდაპირ `provider.rating`-ით ალაგებდა (raw, არა წონიანი) — ახლა `weightedRating()`-ს იყენებს, იმავე ფორმულით, რაც Home-ის ტოპ-ოსტატებს იცავს.
44. **Provider-ის პროფილის სისრულის ბარათი (`ProviderProfileScreen`) — "პროფილი 75%-ით შევსებულია", რა აკლია-ს chip-ებით.** ახალი `src/state/ProviderProfileContext.tsx` — Provider-ისთვის `CustomerProfileContext`-ის ანალოგიური global state, პირველად ავსებს რეალურ (არა ჰარდქოდილ) ხიდს `ProviderSetupScreen`/`ProviderEditProfileScreen`-ს შორის (#12-ში ეს ხარვეზი უკვე იყო ცნობილი). `RegisterScreen`/`GoogleCompleteScreen`-ის provider-branch-ებმაც დაიწყეს სახელი/გვარის ჩაწერა ამ context-ში (აქამდე საერთოდ იკარგებოდა Provider-ისთვის, Customer-ისგან განსხვავებით). `ProviderEditProfileScreen`-ს დაემატა რეალური `hasPhoto` toggle (კამერის ბეჯი აქამდე დეკორატიული იყო, `onPress` არც ჰქონდა) — `ProviderSetupScreen`-ის იგივე პატერნით. **Completeness-ის ფორმულა** (`computeCompleteness`, `ProviderProfileContext.tsx`) — ზუსტად 4 თანაბარწონიანი პუნქტი (25 თითო, მომხმარებლის საკუთარი მაგალითის მიხედვით): ფოტო, აღწერა (`about.trim().length >= 20`), ნამუშევრების ფოტო (portfolio ≥1), სერთიფიკატი (≥1). **სერთიფიკატი ქულაშია, მაგრამ არასდროს სავალდებულო** — `optional: true` ველით მონიშნულია, ვიზუალურად რბილი/დაშტრიხული chip-ითაა (არა warning-ტინტიანი, როგორც დანარჩენი 3), არცერთ ღილაკს/გადამისამართებას არ ბლოკავს. სპეციალობა/არეალი (რომლებიც სავალდებულოა setup-ზე, #13) და გამოცდილება ქულაში არ შედის — მომხმარებლის საკუთარი მაგალითი მხოლოდ ამ 4 ვიზუალურ/ნდობის ველზე იყო ორიენტირებული. ბარათი ჩანს მხოლოდ 100%-ზე დაბლა (`completeness.percent < 100`), დაჭერისას `ProviderEditProfile`-ზე გადადის.
45. **ჩატის ზედა job-ბანერს (`ChatConversationScreen`) დაემატა დინამიური ფასის/სტატუსის ტექსტი ბოლო 'offer' შეტყობინებიდან**, სტატიკური mock `jobBudget` ველის ნაცვლად (რომელიც მთლიანად წაშლილია `ChatEntry`-დან, `src/data/mockChats.ts`) — ის არასდროს იყო რეალურად დაკავშირებული ჩატის ფასის შეთავაზების მექანიზმთან (#2) და კონფლიქტში იყო Customer-ის ფასის-ამორჩევის სრულ აკრძალვასთან (#35). ახლა: `offerStatusText` = "თქვენი შეთავაზება: N ₾" (`latestOffer.from === 'me'`) / "შეთავაზებული ფასი: N ₾" (`'other'`, იმავე ტექსტით რაც CustomerJobDetail-ის `priceLabel()`-ს აქვს, #33-ის ერთგვაროვნების პრინციპით) — `pending`-ზე; "ფასი შეთანხმებულია: N ₾" — `accepted`-ზე; "ფასი უარყოფილია" — `declined`-ზე; offer-ის არარსებობისას არაფერი ემატება (StatusPill-ის job-სტატუსი მარტო რჩება, უცვლელად). ასე Customer/Provider-ს ჩატში ყოფნისას აღარ სჭირდება Job Detail-ზე მუდმივი დაბრუნება ფასის სტატუსის სანახავად.
46. **Provider-ის რეგისტრაციიდან მისამართის ველი და ვალიდაცია მთლიანად ამოღებულია** (`RegisterScreen`-ზეც და `GoogleCompleteScreen`-ზეც) — მომხმარებლის აშკარა მოთხოვნით. Customer-ისთვის ორივე ეკრანზე მისამართი უცვლელად სავალდებულოა. `RegisterScreen`-ში `isProvider`-ზეა პირობითი: `AddressAutocompleteField`-ის რენდერი, `errors.address`, `allValid`-ის address-შემოწმება, `handleSubmit`-ის `setTouched`-ის address key. `GoogleCompleteScreen`-ზე იგივე პრინციპით — Provider-ისთვის ველი საერთოდ არ ჩანს და "გაგრძელება" ღილაკი დაუყოვნებლივ აქტიურია (Google-ის ბარათის confirm-ის მეტი აღარაფერი სჭირდება). ეს არაფერს არ ცვლის Provider-ის სამუშაო არეალის არჩევაში — ეს ისედაც უკვე `ProviderSetupScreen`-ზე ხდებოდა (`RegionAreaPicker`, #9), მხოლოდ ცალკე დუბლირებული "საცხოვრებელი მისამართის" მოთხოვნა მოიხსნა რეგისტრაციის ეტაპიდან.
47. **Job-ის დასრულება რეფაქტორდა ორმხრივ (two-sided) confirmation flow-ად — Provider-ს პირდაპირ დასრულების უფლება აღარ აქვს.** მომხმარებლის აშკარა მოთხოვნით: ადრე Customer-ს (`CustomerJobDetailScreen`) შეეძლო "დიახ, დასრულდა" ღილაკით job პირდაპირ, ცალმხრივად დაესრულებინა — ეს მთლიანად წაშლილია. ახალი state machine (`StatusPill.tsx`-ის `JobStatus` union, დოკუმენტირებული იქვე): `pending` → `active` ("provider_selected") → **`awaiting_customer_confirmation`** (ახალი — Provider-მა დააჭირა "სამუშაო დავასრულე", `ProviderJobDetailScreen`-ის ახალი ღილაკი, რომელიც job-ს არასდროს პირდაპირ "completed"-ზე არ გადაიყვანს) → Customer ხედავს ახალ, გამორჩეულ ბარათს ზუსტად ამ state-ზე ("ოსტატმა სამუშაო დასრულებულად მონიშნა" + "დადასტურება"/"პრობლემა მაქვს") → **"დადასტურება"** პირდაპირ ხსნის სავალდებულო `RatingScreen`-ს, job **`completed`** ხდება მხოლოდ ვარსკვლავის რეალურად გაგზავნის მომენტში (`onRate` callback-ში), არა "დადასტურება"-ზე დაჭერისთანავე; **"პრობლემა მაქვს"** job-ს **`disputed`**-ზე გადაყავს (ახალი state) — არსებული პრობლემის მიზეზის BottomSheet ნაკადი (`PROBLEM_OPTIONS`) უცვლელია, მხოლოდ საბოლოო status შეიცვალა ცალკე ad-hoc `problemSubmitted` boolean-იდან ნამდვილ state-ზე. State-ების სახელები განზრახ არჩეულია მომავალში ბარათით გადახდის უსაფრთხოდ დასამატებლად (მაგ. `payment_pending`/`paid` შეიძლება ჩაისვას `awaiting_customer_confirmation`-სა და `completed`-ს შორის სტრუქტურის შეცვლის გარეშე — job-ის status უკვე string-keyed union-ია, არა boolean flag-ების ნაკრები). გადახდა **არ არის** implementiert ამ ეტაპზე.
    - **ახალი გაზიარებული state, `src/state/JobStatusContext.tsx`** — `Record<jobId, JobStatus>`, `CUSTOMER_JOBS`-იდან seed-ილი, `getStatus`/`setStatus`. ეს აუცილებელი გახდა, რადგან აქამდე job-ის დასრულების state Customer-ისა (`jobCompleted` ლოკალური state) და Provider-ის (`ProviderJobDetail`-ის სტატიკური `mode` route param) მხარეს სრულიად ცალკე ცხოვრობდა, ერთმანეთისთვის უხილავად — ორმხრივი flow ამის გარეშე ფიზიკურად შეუძლებელია.
    - **მონაცემთა მოდელის შეზღუდვა და მისი გადაწყვეტა:** `PROVIDER_FEED` (Provider-ის feed-job-ები, `f1`-`f4`) და `CUSTOMER_JOBS` (Customer-ის job-ჩანაწერები, `j1`-`j3`) ორი დამოუკიდებელი mock-კუნძულია, სხვადასხვა id-სივრცით (ადრეც ასე იყო დაფიქსირებული, #16-17, #30-ის შენიშვნებში) — არ არსებობდა არცერთი მაგალითი, სადაც ორივე მხარეს ერთი და იგივე "namdvili" job ჩანდა. ორმხრივი flow-ის დემონსტრირებისთვის `FeedJob`-ს დაემატა არასავალდებულო `customerJobId?: string` (`mockHomeData.ts`) — შევსებულია მხოლოდ `f2`-ისთვის (`customerJobId: 'j1'`), რომელიც უკვე ნარატიულად იგივე "Provider p1-ის აქტიური სამუშაოა", რასაც `j1` წარმოადგენს Customer-ის მხრიდან. `ProviderJobDetailScreen` ცოცხალ სტატუსს (`getStatus(job.customerJobId)`) მხოლოდ მაშინ კითხულობს/წერს, როცა ეს ბმული არსებობს — წინააღმდეგ შემთხვევაში (მაგ. `mockNotifications.ts`-ის `f1`-ზე მიმთითებელი notification target-ები) ძველებურად სტატიკურ `mode` route param-ზე vardeba (fallback), ისე რომ არსებული notification-deep-link დემო-გზა არ დაზიანდეს. სრული Provider/Customer job-მოდელების გაერთიანება ამ task-ის ფარგლებს სცდება — ეს ცნობილი, უკვე ადრეც დაფიქსირებული შეზღუდვაა.
    - **გვერდითი ეფექტების wiring, staleness-ის თავიდან ასაცილებლად:** `ProviderHomeScreen`-ის "მიმდინარე სამუშაო" ბარათი `completed`-ზე გადასვლის შემდეგ აღარ ჩანს (`currentJobStatus === 'completed' ? null : ...`) და მისი StatusPill ცოცხალია (აღარ არის hardcoded `"active"`); `CustomerJobsScreen`-ის ტაბები/StatusPill-ები ცოცხალ სტატუსს კითხულობენ (`awaiting_customer_confirmation`/`disputed` "დადასტურებული" ტაბშივე რჩება, სამუშაო ჯერ არ დასრულებულა); `ChatConversationScreen`-ის job-ბანერის StatusPill ცოცხალია `chatEntry.jobId`-ით (Provider-ის "სამუშაო დავასრულე"-ს დაუყოვნებლივ აისახავს, არა მხოლოდ Job Detail-ზე). `ProviderMyJobsScreen`-ს **არ** შეხებია — მისი mock (`PROVIDER_MY_JOBS_ACTIVE`/`DONE`) საერთოდ არ შეიცავს `id` ველს, ფიზიკურად ვერ დაუკავშირდება `JobStatusContext`-ს ამ task-ის ფარგლებში (უკვე ადრეც დაფიქსირებული, ცალკე კუნძული, #30).
    - **StatusPill-ის ახალი label-ები:** `awaiting_customer_confirmation` → "ელოდება დადასტურებას" (warning-ტინტი, პატარა pill-ისთვის შემოკლებული — `ProviderJobDetailScreen`-ის სრულ footer/banner ტექსტში კი მომხმარებლის ზუსტი ფრაზა "ელოდება მომხმარებლის დადასტურებას" გამოიყენება), `disputed` → "პრობლემაა" (danger-ტინტი).
48. **Domain models + service layer რეფაქტორინგი — UI ქცევა უცვლელი, მხოლოდ data access-ის არქიტექტურა.** მომხმარებლის აშკარა მოთხოვნით: ახალი `src/types/` (`user.ts`, `provider.ts`, `job.ts`, `quote.ts`, `chat.ts`, `review.ts`) — ყველა ის ტიპი, რაც აქამდე `mockHomeData.ts`/`mockChats.ts`/`mockReviews.ts`/`StatusPill.tsx`-ში, `navigation/types.ts`-ში ან state-კონტექსტების შიგნით იყო გაბნეული (`Provider`, `ProviderProfile`, `FeedJob`, `CustomerJob`, `JobStatus`, `ChatMsg`, `ChatEntry`, `Review`, `RatingData`, `CustomerProfile`, `Role`, ...), ერთ ადგილას შეგროვდა. ახალი `src/services/` (`authService`, `userService`, `jobService`, `quoteService`, `chatService`, `reviewService`, `storageService`) — თითოეული ინტერფეისი + mock-იმპლემენტაცია, ჯერჯერობით mockHomeData/mockChats/mockReviews-ზე აგებული (Firebase ჯერ არ არის დაკავშირებული, ისევე როგორც აქამდე).
    - **სერვისები სინქრონულია, არა Promise-დაფუძნებული** (`authService`-ის გარდა) — ეს განზრახი, დოკუმენტირებული გადაწყვეტილებაა: ეკრანების ამჟამინდელი კოდი მონაცემებს პირდაპირ render-ის დროს კითხულობდა (`Array.find`/`filter`, არა `useEffect`+loading state), ასინქრონულ სერვისებზე გადასვლა ამ ეკრანების სტრუქტურის შეცვლას მოითხოვდა (loading/error state-ების დამატება) — ეს პირდაპირ დაარღვევდა "UI ქცევა არ შეიცვალოს" მოთხოვნას. `authService`-ის მეთოდები კი Promise-ს აბრუნებენ (Firebase Auth-ის რეალურ კონტრაქტთან თანხვედრისთვის) — მაგრამ ეკრანების ხელოვნური loading-დაყოვნება (`setTimeout`, უცვლელი ხანგრძლივობით) ეკრანების მხარეს დარჩა, სერვისი მხოლოდ `setTimeout`-ის შიგნით მოიხმობა fire-and-forget-ად.
    - **Context-ები (`CustomerProfileContext`, `ProviderProfileContext`) userService-ის თხელი რეაქტიული shell გახდა** — `useState`-ის საწყისი მნიშვნელობა და `setProfile`-ის merge-ლოგიკა ახლა `userService.get/updateCustomerProfile`-შია (სინქრონული, იგივე timing), Context მხოლოდ React re-render-ისთვის საჭირო `useState`-ს ინახავს. ორივე default-მონაცემების constant (`DEFAULT_CUSTOMER_PROFILE`/`DEFAULT_PROVIDER_PROFILE`) გადავიდა `userService.ts`-ში.
    - **`providerFeedFilters.ts` წაშლილია**, ჩანთქმული `jobService.ts`-ში (`CURRENT_PROVIDER_ID`, `getOpenProviderFeed`) — ცალკე პატარა ფაილად ყოფნის საჭიროება აღარ იყო, სერვისის ბუნებრივი ნაწილია.
    - **`storageService.ts` შექმნილია, მაგრამ არსად არ არის ინტეგრირებული** — `MediaUploadGrid`-ის `nextMediaItem` helper (სერთიფიკატები/portfolio/RatingScreen-ის ფოტოები) განზრახ დარჩა უცვლელი, რადგან მისი ფერების/id-ების დეტერმინისტული ციკლი ვიზუალურ ქცევას განსაზღვრავს — რენდომული/სერვისზე გადართვა ამ ციკლს დაარღვევდა. `storageService` მხოლოდ მომავალი რეალური Firebase Storage ატვირთვის კონტრაქტს აფიქსირებს.
    - **მიგრირებული ეკრანები/ფაილები (16):** `CustomerHomeScreen`, `ProviderHomeScreen`, `ProviderJobFeedScreen`, `ProviderFeedJobCard`, `ViewProviderProfileScreen`, `CustomerJobDetailScreen`, `JobStatusContext`, `ChatConversationScreen`, `ChatsListScreen`, `CustomerJobsScreen`, `CustomerCategoryScreen`, `ProviderCompletedJobsScreen`, `ProviderJobDetailScreen`, `ProviderMyJobsScreen`, `ProviderReviewsScreen`, `SavedProvidersScreen`, `CustomerTabs`/`ProviderTabs` (unread chat count) — ყველა ეს ფაილი აღარ შემოაქვს `mockHomeData.ts`/`mockChats.ts`/`mockReviews.ts` პირდაპირ, სერვისების მეშვეობით კითხულობს. **ერთადერთი შეგნებული გამონაკლისი:** `CustomerJobDetailScreen`-ის `PHOTO_COLORS` import (mockHomeData.ts-იდან) — ეს დეკორატიული ფერების მასივია (ფოტოს placeholder thumbnail-ებისთვის), არა დომეინის მონაცემი, ცალკე სერვისში/ფაილში გატანა over-engineering იქნებოდა 4-ფერიანი მასივისთვის.
    - **`RegisterScreen`/`LoginScreen`/`ForgotPasswordScreen`-იც `authService`-ს იძახებენ** ახლა (`registerWithEmail`, `signInWithEmail`, `signInWithGoogle`, `sendPasswordReset`) — არსებული `setTimeout`-ის loading-ინდიკატორის შიგნით, ხანგრძლივობის/UI-ის ცვლილების გარეშე.
49. **სერვისის კატეგორიის ემოჯი აიქონები ჩანაცვლდა Lucide ვექტორული აიქონებით** — მომხმარებლის აშკარა მოთხოვნით, "clean/modern/premium" აიქონების სისტემისთვის. ერთადერთი ცენტრალიზებული მაპინგი `src/components/CategoryIcon.tsx`-შია: `CATEGORY_ICON_MAP` (id → Lucide კომპონენტი) + `getCategoryIcon(id)` (ბექვარდ-თავსებადი fallback-ით, Wrench default) ბარე აიქონისთვის, როცა უკვე არსებული ფონის კონტეინერის ფორმა/ზომა (border radius, circle vs square) სხვაგან განისაზღვრება და არ უნდა შეიცვალოს — `CategoryIcon`-ის კომპონენტი (pastel კონტეინერი + აიქონი, `category.dot` ფერით) მხოლოდ იმ ადგილებში, სადაც ადრეც ესეთივე კონტეინერს იყენებდა. `src/data/categories.ts`-ის `Category` ტიპიდან და `src/data/specialties.ts`-ის `SPECIALTIES`-იდან `icon: string` ველი მთლიანად წაშლილია (აღარც საჭიროა) — `SPECIALTIES`-ის id-სივრცე (`plumber`, `electrician`...) `CATEGORIES`-ის id-სივრცისგან (`plumbing`, `electrical`...) განსხვავდება, ამიტომ `getCategoryIcon`-ს დაემატა მცირე ალიას-ცხრილი (`plumber→plumbing`, `electrician→electrical`, `painter→painting`, `drywall→renovation`) იმავე აიქონების ხელახლა გამოსაყენებლად, ცალკე სპეციალობის აიქონების დუბლირების გარეშე. **მიგრირებული ადგილები (12 ფაილი):** `CategoryIcon`/`getCategoryIcon`-ს იყენებენ `ProviderFeedJobCard`, `SpecialtyPickerField`, `CustomerHomeScreen` (top-3 სერვისის წრიული ბადგე), `CustomerCategoriesScreen` (ყველა 15 კატეგორიის წრიული ბადგე), `PostJobScreen` (კატეგორიის ღილაკი + BottomSheet-ის სია), `ChatsListScreen` (ჩატის job-line-ის პატარა inline აიქონი), `ChatConversationScreen` (job-ბანერის კვადრატული აიქონი — გამარტივდა პირდაპირ `<CategoryIcon>`-ზე, ზომები ემთხვეოდა), პლუს ადრევე `CategoryIcon`-ზე აგებული ეკრანები (`CustomerJobDetailScreen`, `CustomerJobsScreen`, `ProviderHomeScreen`, `ProviderJobDetailScreen`, `ProviderCompletedJobsScreen`). ყველა მოთხოვნილი 15 კატეგორია (`plumbing→Wrench`, `electrical→Zap`, `painting→Paintbrush`, `ac→Snowflake`, `heating→Flame`, `furniture→Armchair`, `appliance→PlugZap`, `tile→Grid2X2`, `flooring→PanelsTopLeft`, `doors→DoorOpen`, `locks→LockKeyhole`, `repair→Hammer`, `renovation→House`, `cleaning→Sparkles`, `moving→Package`) ინსტალირებულ `lucide-react-native@1.34.0`-ში ზუსტად ამ სახელებით მოიძებნა — fallback/alternative აირჩევა არცერთისთვის არ დასჭირდა. **განზრახ არ შეხებია:** `ProviderJobDetailScreen`/`ProviderFeedJobCard`-ის "🔥 სასწრაფო" urgent-badge (სტატუსის ინდიკატორია, არა კატეგორია), `RoleSelectScreen`-ის 🏠/🔧 (როლის არჩევანია, არა სერვისის კატეგორია), `mockNotifications.ts`-ის `iconEmoji` (სრულიად ცალკე, ზოგადი შეტყობინების-ტიპის აიქონების სისტემაა, არა `CATEGORIES`-ზე აგებული), `SpecialtyPickerField`-ის "სხვა" მწკრივის ❓ (ზოგადი placeholder, არა კონკრეტული კატეგორიის აიქონი).
50. **Firebase-ის რეალურ backend-თან დაკავშირება დაიწყო — Auth პირველი ეტაპია.** მომხმარებლის მოთხოვნით ("ნელნელა" — ეტაპობრივად), Firestore/Storage/Messaging ჯერ **არ არის** დაკავშირებული, მხოლოდ Authentication. `src/services/authService.ts` სრულად გადავიდა mock-იდან რეალურ `@react-native-firebase/auth`-ზე (**modular API** — `getAuth()`/`createUserWithEmailAndPassword()`/... ცალკეული ფუნქციები, არა ძველი namespaced `auth()...`, რადგან v22+-ში namespaced API deprecated-ია) და `@react-native-google-signin/google-signin`-ზე (v16, `GoogleSignin.signIn()` აბრუნებს `{type:'success', data:{idToken, user}}`-ს, არა ძველ ბრტყელ ფორმას). `AuthService`-ის interface-ი `Promise<void>`-დან `Promise<AuthResult>`-ზე (`{uid, email}`) შეიცვალა — მომავალ Firestore-ეტაპს (`users/{uid}` ჩანაწერის შექმნა) `uid` დასჭირდება.
    - **`GOOGLE_WEB_CLIENT_ID` შევსებულია** `authService.ts`-ში (`463055179499-khj88vj0ts6l2ufvaarnbdgn4f55snkj.apps.googleusercontent.com`) — მომხმარებელმა Firebase Console-ში ჩართო Email/Password და Google provider-ები, ხელახლა ჩამოტვირთა `google-services.json` (`oauth_client`-ში ახლა `client_type: 3` web client ჩანს) და დაამატა Android debug keystore-ის SHA-1 fingerprint. Google Sign-In ახლა სრულად კონფიგურირებულია Android-ზე ტესტვისთვის. **iOS-ისთვის** `GoogleService-Info.plist` ჯერ არ არის ხელახლა ჩამოტვირთილი (ჯერ კიდევ ძველი, `oauth_client`/`REVERSED_CLIENT_ID` არ აქვს) — საჭირო იქნება, როცა iOS-ზე ტესტვას დავიწყებთ.
    - **Email/Password რეგისტრაცია/login/password-reset უკვე სრულად რეალურია** და არ საჭიროებს დამატებით Console-setup-ს Email/Password provider-ის ჩართვის მეტს. Firebase-ის შეცდომის კოდები (`auth/email-already-in-use`, `auth/wrong-password`, `auth/weak-password`...) ქართულ ტექსტად იქარგება `getAuthErrorMessage()`-ით (`authService.ts`) — `RegisterScreen`-ს ახალი error-banner დაემატა (LoginScreen-ის უკვე არსებული `credError` პატერნის მიხედვით), `ForgotPasswordScreen`-საც. `ForgotPasswordScreen`-ი `auth/user-not-found`-საც წარმატებულ state-ად აჩვენებს (უსაფრთხოების პრინციპი — არ ამხელს, ანგარიში არსებობს თუ არა).
    - **`GoogleCompleteScreen`-ი აღარ იყენებს mock Google მომხმარებელს** — რეალურად კითხულობს `authService.getCurrentUser()`-ს (წინა ეკრანზე, `RegisterScreen`-ის Google ღილაკზე, უკვე რეალურად შესულია) და მისი `displayName`/`email`-იდან ავსებს ფორმას.
    - **სავალდებულო, ჯერ დაუხურავი ხვრელი (განზრახ):** `LoginScreen`-ისა და `RegisterScreen`-ის Google ღილაკის "existing user" შემთხვევა ავტორიზაციის შემდეგ **არ გადადის** როლის შესაბამის Home ეკრანზე — ეს `users/{uid}.role`-ის Firestore-წაკითხვას საჭიროებს, რაც შემდეგი ეტაპია. კოდში ეს TODO კომენტარით ცალსახად აღნიშნულია (`handleLogin`-ში) — თავად ავტორიზაცია (credential-ების შემოწმება, შეცდომების დამუშავება) სრულად რეალური და მუშა არის უკვე ახლა.
    - **Registration-ის flow (ორივე როლისთვის) დასრულებულია და მუშაობს ბოლომდე** — Provider/Customer-ის რეგისტრაცია Email/Password-ითაც, Google-ითაც ქმნის რეალურ Firebase Auth ანგარიშს და აგრძელებს არსებულ setup-ნაკადს ჩვეულებრივად (role უკვე registration-ის დროს ცნობილია local navigation param-იდან, Firestore არ სჭირდება). "გასვლა" (`CustomerProfileScreen`/`ProviderProfileScreen`) ახლა რეალურ `authService.signOut()`-საც იძახებს (`GoogleSignin.signOut()`-ითურთ, რომ შემდეგი "Google-ით შესვლა" ანგარიშის არჩევანის დიალოგს ისევ აჩვენებდეს), navigation reset-ის გვერდით.
    - **Native rebuild არ იყო საჭირო ამ ეტაპისთვის** — `@react-native-firebase/auth` და `@react-native-google-signin/google-signin` პაკეტები/config plugin-ები `app.json`-ში უკვე ადრევე იყო დამატებული (როცა პროექტი თავიდან შეიქმნა), ასე რომ არსებულ `android/`-ის native პროექტში native მოდულები უკვე კომპილირებულია.
51. **Firestore-ის პირველი collection — `users/{uid}` (role + საბაზისო identity).** მომხმარებელმა Firebase Console-ში ჩართო Email/Password და Google provider-ები (#50-ის დასასრული), მერე ვთხოვე "დავიწყოთ" Firestore-ზე — ეს არის პასუხი: `providerProfiles`/`jobPosts`/`jobResponses`/`conversations`/`messages`/`reviews` **ჯერ არ არსებობს** (განზრახ, "ნელნელა" პრინციპით) — მხოლოდ `users`. ახალი ტიპი `src/types/user.ts`-ის `UserRecord` (`{role, firstName, lastName, email, defaultAddress}`, `defaultAddress` ცარიელია Provider-ისთვის). `userService.ts`-ს დაემატა (არსებული სინქრონული mock-მეთოდები **უცვლელია**) `createUserRecord`/`getUserRecord`/`updateUserRecord` — რეალურად Firestore-ზე (`getFirestore()`/`doc()`/`getDoc()`/`setDoc(..., {merge:true})`, modular API, `@react-native-firebase/firestore@26`-ის შესაბამისად).
    - **`app.json`-ს დაემატა `@react-native-firebase/firestore` plugin** — ეს, Auth-ისგან განსხვავებით (#50-ის შენიშვნა), **ნამდვილად საჭიროებს ახალ native rebuild-ს**, რადგან Firestore-ის native მოდული აქამდე საერთოდ არ ყოფილა კომპილირებული არსებულ `android/`-ის პროექტში.
    - **ახალი `firestore.rules`** (რეპოს root-ში) — `users/{userId}`-ზე მხოლოდ თავად მომხმარებელს აქვს წვდომა (`request.auth.uid == userId`), ყველა დანარჩენი collection ნაგულისხმევად დაბლოკილია fallback rule-ით. ეს ფაილი მხოლოდ **დოკუმენტაციაა/წყაროა კოდში** — Firebase CLI ამ მანქანაზე დაყენებული არაა, ამიტომ **მომხმარებელს თავად სჭირდება ამ ტექსტის Console-ში ჩასმა** (Firestore Database → Rules ტაბი → publish).
    - **სად იწერება/იკითხება:** `RegisterScreen`/`GoogleCompleteScreen` — `createUserRecord()` წარმატებული auth-ის შემდეგ (ორივე როლისთვის). `LoginScreen`-ის ახალი `completeSignIn()` helper (email-ისთვისაც, Google-ისთვისაც საერთო) — `getUserRecord()`-ით კითხულობს role-ს, შესაბამის Context-ს ჰიდრატებს (`CustomerProfileContext`/`ProviderProfileContext`-ის `setProfile`) და `navigation.reset()`-ით სწორ Home-ზე გადადის — **ეს ხურავს #50-ში ღიად დატოვებულ ხვრელს** ("login-ის შემდეგ სად წავიდეს" TODO-ს მეტი აღარ სჭირდება). თუ ჩანაწერი არ მოიძებნა (ძველი Firebase Auth ანგარიში ამ ფუნქციამდე, ან Google-ით შესვლა Login-იდან ახალი ანგარიშისთვის) — `signOut()`-ით უკან ბრუნდება და მკაფიო შეცდომას აჩვენებს ("jერ დარეგისტრირდი"), ნახევრად-authenticated state-ს არ ტოვებს. `CustomerEditProfileScreen`-ის რეალურ (მეორე მცდელობის) შენახვაზე ახლაც წერს Firestore-ში (`updateUserRecord`) — ლოკალური Context-ის განახლება optimistic/მყისიერია, Firestore-ის ჩავარდნა UI-ს არ ბლოკავს (მხოლოდ ჩუმად "ჩამორჩება", მომავალში retry საჭირო იქნება).
    - **განზრახ არ შეხებია ამ ეტაპზე:** Provider-ის სრული პროფილი (`ProviderProfileContext`-ის specialty/areas/about/photo/certs/portfolio) კვლავ ლოკალური mock არის — `providerProfiles` collection შემდეგი ბუნებრივი ეტაპია. აპლიკაციის cold-start session restore (app-ის ხელახლა გახსნისას ავტომატურად Welcome-ის გამოტოვება, თუ Firebase Auth-ს უკვე აქვს დამახსოვრებული სესია) **ასევე არ არის აშენებული** ამ ეტაპზე — RootNavigator ყოველთვის Welcome-ით იწყება, თუნდაც სესია აქტიური იყოს ფონზე. ორივე ცალსახად აღნიშნულია, როგორც ბუნებრივი შემდეგი ნაბიჯები.
52. **ბექენდი გადავიდა Firebase-დან Supabase-ზე — Firebase Auth-ის ახალი reCAPTCHA Enterprise abuse-დაცვა ვერასოდეს გაატანა.** #50-#51-ში აშენებული Firebase Auth + Firestore `users` collection სრულად მუშა იყო კოდის დონეზე, მაგრამ **რეალურ მოწყობილობაზე/ემულატორზე ვერასდროს ჩაივლიდა** — `createUserWithEmailAndPassword`-იც და Google-ის `signInWithCredential`-იც მუდმივად აბრუნებდა `RecaptchaCallWrapper: ... API key not valid`-ს (`firebase-auth@@24.2.0`). ამოწურული, დოკუმენტირებული Google Cloud Console-ის ყველა შესაძლო გამოსწორება (API key-ის Application/API restrictions ორივე მიმართულებით, ყველა საჭირო API-ს ჩართვა, ხელით შექმნილი reCAPTCHA Enterprise key, Firebase Console-ის SHA-1+SHA-256, Blaze billing-ზე გადასვლა, სერტიფიცირებული Play Store emulator, `auth().settings.appVerificationDisabledForTesting`) — არცერთმა არ უშველა, იდენტური შეცდომა მეორდებოდა. ამის შემდეგ მომხმარებლის გადაწყვეტილებით (რომელმაც თავიდანვე Supabase სურდა) გადავინაცვლეთ Supabase-ზე.
    - **რატომ Supabase კონკრეტულად ამ პრობლემას აგვარებს:** `@supabase/supabase-js` REST/WebSocket API-ზეა აგებული, არანაირ native Google Play Integrity/reCAPTCHA Enterprise მექანიზმს არ იყენებს Auth-ისთვის — იმუშავებს იდენტურად ემულატორზეც, ფიზიკურ მოწყობილობაზეც, web-ზეც, Google Cloud Console-ის კონფიგურაციის გარეშე.
    - **სერვისის ფენის დიზაინი (#48) გადამწყვეტად გამოსადეგი აღმოჩნდა** — ეკრანები (`RegisterScreen`, `LoginScreen`, `GoogleCompleteScreen`, `CustomerEditProfileScreen`, `CustomerProfileScreen`, `ProviderProfileScreen`) **საერთოდ არ შეცვლილა**; მხოლოდ `src/services/authService.ts`-ისა და `userService.ts`-ის Firestore-ნაწილის შიდა იმპლემენტაცია ჩანაცვლდა Supabase-ით, `AuthResult`/`UserRecord` ტიპების იგივე ფორმის დაცვით.
    - **`authService.ts`** — სრულად გადაწერილია `@supabase/supabase-js`-ზე: `supabase.auth.signUp`/`signInWithPassword`/`signInWithIdToken` (Google, `@react-native-google-signin/google-signin`-იდან მიღებული `idToken`-ით — თავად Google Sign-In-ის ნაწილი უცვლელი დარჩა, მხოლოდ შედეგის დანიშნულება შეიცვალა Firebase-დან Supabase-ზე) / `resetPasswordForEmail` / `signOut`. ახალი `AppUser` ტიპი (`{uid, email, displayName}`) `FirebaseUser`-ის მაგივრად. **`getCurrentUser()` სინქრონული დარჩა** (#48-ის პრინციპით, `GoogleCompleteScreen`-ს სინქრონულად სჭირდება) — Supabase-ის auth-js-ს, Firebase-ისგან განსხვავებით, არ აქვს სინქრონული getter, ამიტომ `authService.ts`-ში დაემატა module-level `cachedUser`, რომელიც ივსება `onAuthStateChange`-ითაც და პირდაპირ თითოეული წარმატებული auth call-ის შემდეგაც. Georgian error-message mapping (`getAuthErrorMessage`) გადაკეთდა Supabase-ის `error.message` სტრიქონებზე (Firebase-ის `error.code`-ის მაგივრად).
    - **`userService.ts`** — `createUserRecord`/`getUserRecord`/`updateUserRecord` ახლა წერენ/კითხულობენ Supabase-ის Postgres `users` ცხრილს (`supabase.from('users')`), snake_case row shape-ის (`first_name`/`last_name`/`default_address`) camelCase `UserRecord`-თან კონვერტაციით სერვისის საზღვარზე (`fromRow()` helper) — დანარჩენი (sync mock) მეთოდები უცვლელია, ისევე როგორც #51-ში.
    - **ახალი `src/services/supabaseClient.ts`** — `createClient()`, `@react-native-async-storage/async-storage`-ით სესიის შესანახად (`persistSession`/`autoRefreshToken`). `index.ts`-ის თავში დაემატა `import 'react-native-url-polyfill/auto'` (Supabase-ის RN-ზე მუშაობის სავალდებულო წინაპირობა).
    - **წაშლილია მთლიანად:** `@react-native-firebase/{app,auth,firestore,messaging,storage}` (package.json + node_modules), `google-services.json`, `GoogleService-Info.plist`, `firestore.rules`, `app.json`-ის Firebase plugin-ები და `googleServicesFile` ველები. `@react-native-google-signin/google-signin` **დარჩა** (Google-ის native ID token-ის მისაღებად კვლავ საჭიროა, უბრალოდ token ახლა Supabase-ს ეგზავნება Firebase-ის მაგივრად).
    - **მომხმარებლის მხრიდან საჭირო ნაბიჯები (მიწოდებულია ცალკე):** `users` ცხრილის + RLS policy-ების შექმნა Supabase SQL Editor-ში, Authentication → Providers → Email-ის "Confirm email" გამორთვა (რომ registration-ის მერე დაუყოვნებლივი login-ის არსებული UX არ დაირღვეს — Supabase-ს default-ად ჩართული აქვს), და Authentication → Providers → Google-ის ჩართვა იმავე `GOOGLE_WEB_CLIENT_ID`-ით (+ Google Cloud Console-ის Client Secret) Google Sign-In-ის სამუშაოდ.
    - **`docs/firebase-setup.md`** მოძველებულია (ისტორიული საცნობარო დოკუმენტია, Firebase-ის კონფიგურაციის ეტაპისთვის) — არ წაშლილა, მაგრამ აღარ ასახავს რეალურ სისტემას.
53. **Supabase-ის მეორე ცხრილი — `provider_profiles` (Provider-ის საკუთარი პროფილის გაფართოება).** #52-ის Auth+`users` მიგრაციის შემდეგ ლოგიკური შემდეგი ნაბიჯი — Customer-ის მხარეს ყველა პროფილის ველი უკვე `users`-შია (#51/#52), Provider-ს კი დამატებით სჭირდება specialty/areas/experience/about/ფოტო/სერთიფიკატები/portfolio/sqm-ფასები (#44-ის `ProviderProfileContext`-ის ველები), რაც აქამდე მთლიანად ლოკალური mock იყო. `first_name`/`last_name` **განზრახ არ არის დუბლირებული** ახალ ცხრილში — ეს ველები single source of truth-ად `users`-ში რჩება (ორივე role-ისთვის საერთო), `ProviderProfile` TS ტიპი კი (Context-ის/ეკრანების state-ისთვის) უცვლელად შეიცავს მათ ორივეს — ახალი `ProviderProfileRecord` ტიპი (`userService.ts`) არის `Omit<ProviderProfile, 'firstName'|'lastName'>`, ზუსტად ამ დუბლირების ასაცილებლად.
    - **`userService.ts`-ს დაემატა** `getProviderProfileRecord(uid)`/`upsertProviderProfileRecord(uid, record)` (async, Supabase-ზე) — არსებული სინქრონული mock-მეთოდები (`getProviderProfile`/`updateProviderProfile`, `ProviderProfileContext`-ის local state-ისთვის) **უცვლელია**, ზუსტად #51-ის იგივე additive-only პრინციპით. `upsert` (არა ცალკე create/update), რადგან `ProviderSetupScreen` პირველად ქმნის ჩანაწერს და `ProviderEditProfileScreen` შემდეგ ანახლებს — ორივეს იგივე მეთოდი ემსახურება.
    - **`ProviderSetupScreen`-ის "პროფილის შექმნა"** ახლა რეალურად წერს Supabase-ში (`handleContinue` async გახდა, `TODO: Firestore`-ის კომენტარი მოცილებულია) — **ეს, CustomerEditProfile-ისგან/ProviderEditProfile-ისგან განსხვავებით, hard-fail-ია**: თუ Supabase-ის ჩაწერა ჩავარდა, `ProviderHome`-ზე არ გადადის, ახალი `InlineBanner`-ის error+"თავიდან ცდა" ჩნდება (`ProviderEditProfileScreen`-ის უკვე არსებული პატერნით ნასესხები `InlineBanner` კომპონენტი) — შეგნებული განსხვავება, რადგან ეს ერთჯერადი სავალდებულო setup-გეიტია (#13), არა შემდგომი რედაქტირება, რომლის დაკარგვასაც optimistic-local-update-ით "ატანა" შეიძლება.
    - **`ProviderEditProfileScreen`-ის "ცვლილებების შენახვა"** (მეორე, წარმატებული მცდელობის შემდეგ — არსებული "პირველი ცდა ყოველთვის ვარდება" დემო-ქცევა უცვლელია) ახლა წერს ორივეგან: `userService.updateUserRecord(uid, {firstName, lastName})` (`users`-ში) და `userService.upsertProviderProfileRecord(uid, {...})` (`provider_profiles`-ში) — `CustomerEditProfileScreen`-ის იგივე optimistic-local-update + fire-and-forget-background-persist პრინციპით (ჩავარდნისას UI არ იბლოკება, silently ჩამორჩება).
    - **`LoginScreen`-ის `completeSignIn()`-ს** Provider-ის შტოზე დაემატა `getProviderProfileRecord(uid)`-ის fetch და `setProviderProfile(...)`-ით ჰიდრატაცია (`users`-იდან წამოღებული firstName/lastName-ის დამატებით) — login-ის შემდეგ `ProviderProfileScreen`/`ProviderEditProfileScreen` მაშინვე რეალურ, შენახულ მონაცემებს აჩვენებენ ცარიელი/default state-ის ნაცვლად.
    - **განზრახ არ შეხებია:** `Provider` ტიპი (საჯარო directory entity — rating/reviews/jobs/location, Customer-ის Browse/კატეგორიის/საჯარო-პროფილის ეკრანებზე) კვლავ mock `PROVIDERS`-ზეა აგებული (`listProviders`/`getProviderById`) — ეს დამოკიდებულია `job_posts`/`reviews` ცხრილებზეც (მომავალი ეტაპები), ამ ორ ტიპს (`Provider` საჯარო vs `ProviderProfile` რედაქტირებადი) შორის კავშირის აშენება ცალკე, უფრო დიდი ამოცანაა, ამ ეტაპის ფარგლებს სცდება.
    - SQL + RLS policy-ები მომხმარებელს მიეწოდა ცალკე (Supabase SQL Editor-ში გასაშვებად) — `provider_profiles`-ის owner-only წვდომა, ზუსტად `users`-ის (#51/#52) იგივე pattern-ით.
54. **Supabase-ის მესამე ცხრილი — `job_posts`, "ეტაპი A" (Customer-ის მხარე მხოლოდ).** მომხმარებლის მოთხოვნა იყო "ყველაფრის ნაბიჯ ნაბიჯ გადატანა, სტატიკური მონაცემები აღარ იყოს" — `job_posts` კატეგორიულად უფრო დიდი ცვლილებაა, ვიდრე `users`/`provider_profiles`, რადგან job-ების **სია** (არა მხოლოდ ცალკეული action) იკითხება ეკრანის ჩატვირთვისთანავე, რაც პირველად შემოაქვს ნამდვილი (fetch-ზე დაფუძნებული) loading state ეკრანის დონეზე — #48-ის "ეკრანების სტრუქტურა უცვლელი რჩება" პრინციპი აქ ნაწილობრივ იცვლება, განზრახ, რადგან სია სინქრონული mock-ის გარეშე ფიზიკურად ვერ იმუშავებს. ამიტომ ეს ეტაპი **განზრახ გაიყო ორ ნაწილად:**
    - **A (ეს ეტაპი):** Customer წერს (`PostJobScreen`) და კითხულობს (`CustomerJobsScreen`/`CustomerJobDetailScreen`) **მხოლოდ საკუთარ** job-ებს.
    - **B (მომავალი, ცალკე ეტაპი):** Provider-ის Job Feed (`ProviderJobFeedScreen`/`ProviderHomeScreen`) — ყველა Customer-ის ღია job-ების ნახვა, დამატებით RLS policy-ს საჭიროებს (Provider-მა სხვისი ჩანაწერიც უნდა დაინახოს, არა მხოლოდ საკუთარი) — ცალკე გადაწყვეტილება/სესია, ამ ეტაპში არ განხორციელებულა.
    - **`jobService.ts`-ს დაემატა** `createCustomerJob(customerId, input)`/`listMyJobPosts(customerId)` (async, Supabase-ზე) — არსებული სინქრონული mock-მეთოდები (`listCustomerJobs`/`getCustomerJobById`/`listProviderFeed`/`getOpenProviderFeed`/`getFeedJobById`/`listMyActiveJobs`/`listMyDoneJobs`) **უცვლელია**, #51-ის იგივე additive-only პრინციპით — `FeedJob`/`MyJobRow`-ზე დაფუძნებული ყველაფერი (Provider-ის მხარე მთლიანად) კვლავ mock-ია.
    - **`PostJobScreen`-ის "გამოქვეყნება"** ახლა რეალურად წერს Supabase-ში (`handlePublish` async გახდა, ახალი error-state+`InlineBanner`, `ProviderSetupScreen`-ის (#53) იგივე hard-fail პატერნით — publish-ის ჩავარდნისას success-ეკრანზე არ გადადის). **"title" ველი აპში საერთოდ არ არსებობს** (#23-ის მიხედვით ამოღებულია) — ახლად შექმნილი job-ისთვის `title` აღებულია არჩეული კატეგორიის label-იდან (`CATEGORIES.find(...).label`), მოკლედ, ცალკე ველის დამატების გარეშე. **ფოტოები ჯერ არ იწერება** ცხრილში საერთოდ (Storage-ზე დაკავშირება ცალკე, მომავალი ეტაპია) — `job_posts`-ს ფოტოს ველი არც კი აქვს ამ ეტაპზე.
    - **"მოთხოვნის ნახვა" ღილაკი success-ეკრანზე** ადრე hardcoded იყო (`jobId: 'j2'`, ყოველთვის ერთი და იგივე mock job ჩანდა, არა რეალურად შექმნილი) — ეს ბაგი გასწორდა, ახლა ნავიგაცია რეალურად შექმნილი job-ის id-ით ხდება.
    - **`CustomerJobsScreen`-ის "ჩემი განცხადებები"** ტაბი აღარ კითხულობს mock `CUSTOMER_JOBS`-ს — მხოლოდ Supabase-იდან Customer-ის საკუთარ job-ებს (`useFocusEffect`, არა plain `useEffect` — Bottom Tab-ის ეკრანები default-ად მთელი session-ის განმავლობაში მონტირებული რჩება, ამიტომ ტაბზე დაბრუნებისას refetch სჭირდება ახალი, ახლახან გამოქვეყნებული job-ის დასანახად). ეს ეკრანის უკვე არსებული fake `setTimeout`-loading-სკელეტონი (`Skeleton`-კომპონენტით) პირდაპირ ჩანაცვლდა ნამდვილი async fetch-ით — ვიზუალურად უცვლელი.
    - **`CustomerJobDetailScreen`-ს (1175 ხაზი, `interestedList`/rating/completion-ის სრული flow, `JobStatusContext`-ზე მჭიდროდ დამოკიდებული) მხოლოდ ერთი ხაზი შეეხო**, დანარჩენი მთლიანად უცვლელია — `RootStackParamList`-ის `CustomerJobDetail`-ს დაემატა არასავალდებულო `job?: CustomerJob` param (`navigation/types.ts`). `CustomerJobsScreen`/`PostJobScreen` (ორივემ უკვე ხელთ აქვთ real `CustomerJob` ობიექტი — სიიდან ან შექმნის pასუხიდან) ამ param-ით პირდაპირ გადასცემენ, ხელახალი ცალკე async fetch-ის გარეშე. ეკრანი კითხულობს `route.params.job ?? jobService.getCustomerJobById(...) ?? jobService.listCustomerJobs()[1]` — ანუ **fallback-chain**, ზუსტად #47-ის `ProviderJobDetailScreen`-ის `customerJobId`-ის იგივე პრინციპით: რეალური მონაცემი, თუ არსებობს, თორემ ძველი mock/demo ქცევა (notification deep-link-ები `jobId: 'j1'`-ის მსგავსი მითითებებით კვლავ იმუშავებს). `interestedList`/`ratingData`/`sharedStatus` (quoteService/reviewService/JobStatusContext) რეალურ (UUID) job-ებზე უბრალოდ ცარიელ/undefined მნიშვნელობებზე vardebიან (`INTERESTED_PROVIDERS[uuid] ?? []` და ა.შ.) — უსაფრთხოდ, crash-ის გარეშე, უბრალოდ "ჯერ არავინ დაინტერესებულა" ვიზუალურ მდგომარეობას აჩვენებს ახლად შექმნილ job-ზე, რაც სწორიცაა.
    - SQL + RLS: `job_posts` — მხოლოდ `customer_id = auth.uid()` წვდომა (Provider-ის საჯარო წაკითხვა "ეტაპი B"-მდე არ არსებობს).
55. **`job_posts`-ის "ეტაპი B" — Provider-ის Job Feed, ღია job-ების საჯარო წაკითხვა.** #54-ის პირდაპირი გაგრძელება, იმავე სესიაში. ორი ახალი ტექნიკური საკითხი გადაწყდა:
    - **RLS-ის ორმაგი SELECT policy** — `users` ცხრილი (auth.uid()=id) ცალკეა `job_posts`-ისგან, ამიტომ `job_posts`-ს დაემატა მეორე policy: `exists (select 1 from users where id=auth.uid() and role='provider')` (status='pending'-ის პირობით) — Postgres-ში ერთი და იმავე command-ის (SELECT) რამდენიმე policy OR-ით ერთიანდება, ასე რომ Customer კვლავ მხოლოდ საკუთარს ხედავს (ნებისმიერი სტატუსით), Provider კი — ნებისმიერი Customer-ის ღია (`pending`) job-ს.
    - **Customer-ის სახელის დუბლირება (denormalization)** — Provider-ის Job Feed-ს სჭირდება Customer-ის სახელის ჩვენება (`FeedJob.customer`, #30-ის მოთხოვნა), მაგრამ PostgREST-ის embedded join-ი `users` ცხრილთან RLS-ს პატივს სცემდა და Provider-ისთვის NULL დაბრუნდებოდა (Provider არ არის იმ Customer-ის `users`-row-ის owner-ი). ამის მაგივრად `job_posts`-ს დაემატა `customer_name` სვეტი, შევსებული `PostJobScreen`-ის მიერ თავად შექმნისას (`useCustomerProfile()`-იდან უკვე ხელთ არსებული `firstName`/`lastName`) — RLS/join-ის სირთულის გვერდის ავლა, join-ის ნაცვლად უბრალო denormalized ველი.
    - **`jobService.ts`-ს დაემატა** `getOpenProviderFeedPosts(): Promise<FeedJob[]>` — კითხულობს `job_posts`-ს `status='pending'`-ით, თარგმნის `FeedJob`-ად. ველები, რომლებიც job_posts-ში არ არსებობს (`interested`, `urgent`, `hasPhoto`) — ნაგულისხმევად `0`/`false`/`false` (job_responses/Storage მომავალი ეტაპებია). `ago` გამოითვლება `created_at`-იდან ახალი `formatAgo()` helper-ით (წუთი/საათი/დღე). **`customerJobId` ახლა უბრალოდ job-ის საკუთარი `id`-ის ტოლია** — რეალური job_posts ერთადერთი ცხრილია (არა Customer/Provider-ის ორი ცალკე mock-კუნძული, რასაც #16-17/#30/#47 არაერთხელ აფიქსირებდა როგორც ცნობილ შეზღუდვას) — ეს კონკრეტული, ხანგრძლივად დოკუმენტირებული შეზღუდვა **რეალური მონაცემებისთვის საბოლოოდ მოგვარებულია** (mock demo-მონაცემები კვლავ ძველებურად ორ ცალკე კუნძულადვე რჩება, უცვლელი).
    - **`ProviderJobFeedScreen`/`ProviderHomeScreen`** — `jobService.getOpenProviderFeed()` (sync mock) ჩანაცვლდა `getOpenProviderFeedPosts()`-ით (`useFocusEffect` + `ProviderFeedJobCardSkeleton`, `CustomerJobsScreen`-ის (#54) იგივე pattern-ით). `ProviderHomeScreen`-ის **"მიმდინარე სამუშაო" ბარათი (`currentJobCandidate`/`CURRENT_PROVIDER_ID`, #30/#47) განზრახ არ შეხებია** — ეს ცალკე, თვითკმარი mock-დემო მექანიზმია, `listProviderFeed()`-ის (სრული, გაუფილტრავი mock სია) მიხედვით მუშაობს დამოუკიდებლად ღია-job-ების ცხრილისგან.
    - **`ProviderJobDetailScreen`-ს ერთი ხაზი შეეხო** — ზუსტად #54-ის `CustomerJobDetailScreen`-ის იგივე ტექნიკა: `RootStackParamList`-ის `ProviderJobDetail`-ს დაემატა არასავალდებულო `job?: FeedJob`, ეკრანი კითხულობს `route.params.job ?? providerFeed.find(...) ?? providerFeed[0]` — დანარჩენი 668-ხაზიანი ფაილი (ორმხრივი დასრულების flow, #47) მთლიანად უცვლელია.
56. **`job_responses` ცხრილი — Provider-ის "დაინტერესება"-ს რეალური ჩაწერა/წაკითხვა.** #55-ის პირდაპირი გაგრძელება (იმავე სესიაში). ორივე მიმართულება აშენდა, მაგრამ Customer-ის მხარეს **შეგნებული, დოკუმენტირებული გამარტივებით**:
    - **დენორმალიზაცია განმეორდა** — ისევე როგორც `job_posts.customer_name` (#55), `job_responses`-ს აქვს `provider_name`/`provider_initials`/`provider_color` სვეტები, შევსებული Provider-ის მიერ თავად "დაინტერესებისას" (`useProviderProfile()`-იდან) — Customer-ს არ სჭირდება `users`/`provider_profiles`-ის RLS-ის გვერდის ავლა Provider-ის სახელის სანახავად.
    - **`quoteService.ts`-ს დაემატა** `expressInterest(jobId, provider, offeredPrice?)`, `listMyResponseJobIds(providerId)` (Provider-ის საკუთარი "უკვე დაინტერესებული ვარ" state-ის აღსადგენად ეკრანის ხელახლა გახსნისას), `listResponsesForJob(jobId)` (Customer-ის დეტალის ეკრანისთვის) — არსებული სინქრონული `getQuotesForJob` (mock) **უცვლელია**.
    - **წერის მხარე (Provider) სრულად რეალურია სამივე ადგილას:** `ProviderJobDetailScreen`-ის "დაინტერესების გაგზავნა" (BottomSheet-ის ფასით), `ProviderHomeScreen`-ისა და `ProviderJobFeedScreen`-ის სწრაფი "დაინტ. ვარ" ღილაკი ბარათზე (ფასის გარეშე, არსებული ქცევის მიხედვით) — ორივე ადგილას optimistic local-state + fire-and-forget Supabase-ის ჩაწერა (#51-ის იგივე პრინციპი). სამივე ეკრანი ასევე ჰაიდრეითდება `listMyResponseJobIds`-ით ჩატვირთვისას/ფოკუსზე, რომ "უკვე დაინტერესებული" state არასწორად არ დაუბრუნდეს `false`-ს ეკრანის ხელახლა გახსნისას.
    - **წაკითხვის მხარე (Customer, `CustomerJobDetailScreen`) — ნაწილობრივ რეალურია, შეგნებული კომპრომისით.** `interestedList` `const`-იდან `useState`-ად გადაკეთდა (საწყისი მნიშვნელობა კვლავ სინქრონული mock-lookup, mock demo j1/j2/j3-ის დაცვისთვის), `useEffect`-ი შემდეგ ცდილობს რეალურ `listResponsesForJob(job.id)`-ს და **მხოლოდ არა-ცარიელ პასუხზე** ანაცვლებს (`real.length > 0` guard) — ასე mock demo-ს ცარიელი რეალური query არ შლის, ხოლო რეალურ job-ზე რეალური responses ნორმალურად ჩნდება. **დანარჩენი 1175-ხაზიანი ფაილი (sort/select/rating/completion flow) მთლიანად უცვლელია.**
    - **რეალურ response-ზე აგებული `Provider` ობიექტი (`quoteService.fromJobResponseRow`) მხოლოდ დენორმალიზებულ ველებს (name/initials/color) ავსებს რეალურად** — `rating`/`reviews`/`years`/`verified`/`sqmPrice`/`areas`/`skills` ნაგულისხმევებზეა (`0`/`false`/`[]`/`''`), რადგან საჯარო Provider დირექტორია (`listProviders`/`getProviderById`, #48-ის შენიშვნა) ჯერ კიდევ mock-ია. ეს **უსაფრთხოდ იმუშავებს არსებულ UI-ში დამატებითი ცვლილების გარეშე** — `isNewProvider`-ის (#42) "ახალი ოსტატი" badge უკვე გამართულად ამუშავებს `reviews === 0` შემთხვევას (ვარსკვლავების ნაცვლად), დახარისხება (price/rating/experience) კვლავ მუშაობს (უბრალოდ რეალურ providers-ს შორის ტოლობაა 0-ზე, სანამ საჯარო დირექტორია არ აშენდება). `job.interested`-ის რიცხვი (`ProviderJobDetailScreen`-ის სტატისტიკის ბარათი) კვლავ `0`-ზეა default-ად (რეალური აგრეგირებული count არ არის აშენებული ამ ეტაპზე) — ცნობილი, მიღებული გამარტივება.
    - SQL + RLS: `job_responses` — Provider წერს/კითხულობს საკუთარს (`provider_id = auth.uid()`), Customer კითხულობს საკუთარი `job_posts`-ის responses-ს (`exists`-subquery `job_posts.customer_id = auth.uid()`-ით).
57. **`messages` ცხრილი — ჩატის ტექსტური შეტყობინებები, "ეტაპი A" (Realtime-ისა და structured "offer"-ის გარეშე).** `ChatConversationScreen` (811 ხაზი) 10-ვე navigation call site-იდან იხსნება (`CustomerJobDetailScreen`, `ProviderHomeScreen`, `ProviderJobFeedScreen`, `ProviderJobDetailScreen`, `CustomerJobsScreen`, `CustomerHomeScreen`, `ChatsListScreen`, `SavedProvidersScreen`, `ViewProviderProfileScreen`, `NotificationsScreen`) — ყველგან `chatId` route param-ად გადაეცემა "მეორე მხარის id"-ად, მაგრამ **ამ id-ის უმეტესობა კვლავ mock-სტაბია** (`'p1'`–`'p5'`, `'c1'`) — რეალურ Supabase UUID-ს მხოლოდ ერთი ნაკადი გადასცემს დღეს: `CustomerJobDetailScreen`-ის დაინტერესებული ოსტატის ბარათის "ჩატი" ღილაკი (`provider.id`, #56-ის `job_responses`-იდან, ნამდვილი `auth.users` UUID).
    - **`conversations` ცხრილი განზრახ არ აშენებულა** — "საუბრის" იდენტობა უბრალოდ `(customer_id, provider_id)` წყვილია `messages`-ის ორ სვეტზე პირდაპირ, ცალკე conversation-row/get-or-create round-trip-ის გარეშე. ეს გადამწყვეტად ამარტივებს ინტეგრაციას: ვერცერთი 10-ვე call site-ს არ სჭირდება ცვლილება (async "მოძებნე ან შექმენი საუბარი" ნაბიჯი ნავიგაციამდე) — chatId კვლავ სინქრონულად, უცვლელად მუშავდება ყველგან.
    - **`ChatConversationScreen`-ს მხოლოდ დამატებები შეეხო, არაფერი წაშლილა.** ახალი `UUID_RE` შემოწმებით დგინდება, არის თუ არა `chatId` ნამდვილი UUID (`isReal`) — თუ არა, ეკრანი ზუსტად ისე იქცევა, როგორც აქამდე (`chatService.getMessages(chatId)`-ის mock სია, ლოკალური `setTimeout`-ის "gაიგზავნა" სიმულაცია). თუ `isReal`-ია, ახალი `useEffect` კითხულობს `chatService.listRealMessages(customerId, providerId, myUid)`-ს (ცვლის mock-ის ნაცვლად — ნამდვილი UUID-ისთვის mock-ში ისედაც არაფერი მოიძებნება) და `sendMsg` პარალელურად წერს `sendRealMessage`-ით (optimistic local-append უცვლელია, უბრალოდ "sent"/"failed" state-ის წყარო რეალურ Supabase-ის პასუხზეა, ნაცვლად ყოველთვის-წარმატებული fake timeout-ისა).
    - **Structured "offer" ტიპის შეტყობინება (D3, #2/#45) და Realtime — ორივე შემდეგი, ცალკე ეტაპია ("ეტაპი B")** — ეს session ტექსტურ შეტყობინებებზე შეჩერდა. ამჟამად ახალი შეტყობინების სანახავად საჭიროა ეკრანის ხელახლა გახსნა (ფოკუსზე refetch არაა დამატებული ჯერ — chat-screen ერთხელ იტვირთება mount-ზე, არა focus-ზე, ორმხრივი ცოცხალი საუბრისთვის Realtime-ია საჭირო, არა focus-refetch).
    - SQL + RLS: `messages` — მონაწილე (customer_id ან provider_id) კითხულობს, მონაწილე წერს მხოლოდ sender_id=auth.uid()-ით.
58. **`reviews` ცხრილი — ბოლო დაგეგმილი ცხრილი, job-ის დასრულების შეფასების რეალური ჩაწერა/წაკითხვა.** ისევ იგივე დენორმალიზაციის პრინციპი — `customer_name` პირდაპირ `reviews`-ის მწკრივშივეა (Customer-ის მიერ თავად შეფასების გაგზავნისას, `useCustomerProfile()`-იდან) — Provider-ს/სხვა Customer-ებს არ სჭირდებათ `users`-ის RLS-ის გვერდის ავლა შემფასებლის სახელის სანახავად.
    - **ახალი გაზიარებული `src/utils/isUuid.ts`** — მცირე helper (`isUuid(value)`), რომელიც ადრე ოთხჯერ თითქმის იდენტურად გამეორდა ამ სესიაში (job_responses, messages, ახლა reviews — "არის თუ არა ეს id ნამდვილი Supabase UUID თუ mock-სტაბი") — ამოტანილია საერთო ფაილში, `ChatConversationScreen`-ის ადრინდელი inline `UUID_RE` კი განზრახ **უცვლელი დარჩა** (არსებული, უკვე მუშა კოდის შეხება მხოლოდ ამ refactor-ისთვის რისკის ღირებული არ იყო).
    - **წერის მხარე (`CustomerJobDetailScreen`-ის `openRating`-ის `onRate` callback)** — არსებული ლოკალური `setRatingData`/`setStatus(job.id, 'completed')` **უცვლელია** (UI-ს დაუყოვნებლივი feedback), დამატებით, თუ `job.id`-იც და არჩეული Provider-ის `id`-იც ნამდვილი UUID-ებია (`isUuid`), fire-and-forget-ად იწერება `reviewService.submitReview(...)`-ითაც.
    - **წაკითხვის მხარე — მხოლოდ `ProviderReviewsScreen`-ს შეეხო, `ViewProviderProfileScreen`-ს განზრახ არა.** `ProviderReviewsScreen`-ი ("ჩემი შეფასებები") ყოველთვის ავტორიზებულ, **რეალურ** `auth.uid()`-ს კითხულობს (`authService.getCurrentUser()`) — უსაფრთხო overlay, საწყისი mock 'p1'-ის დემო-შეფასებებზე. **`ViewProviderProfileScreen`-ს კი შეგნებულად არ შევხებივარ** — ეს ეკრანი `userService.getProviderById(route.params.id)`-ს კითხულობს, რომელიც კვლავ მთლიანად mock `PROVIDERS`-ზეა აგებული (საჯარო დირექტორია, #48-ის ცნობილი შეზღუდვა) — real UUID route param-ზე ეს fallback `listProviders()[0]`-ზე (მცდარ, არასწორ mock provider-ზე) გადავარდებოდა, ხოლო რეალური reviews swap ამ არასწორ provider-ს დაუკავშირდებოდა. სისწორის დარღვევის თავიდან ასაცილებლად ეს ეკრანი **მთლიანად mock-ზე დარჩა**, ნაწილობრივი/შეუსაბამო რეალური მონაცემის ნაცვლად — დარჩება მომავალი, უფრო დიდი "საჯარო Provider დირექტორიის" ეტაპისთვის.
    - **ფოტოს ატვირთვა (RatingScreen) კვლავ mock/placeholder-ია** — `photos` ველი `reviews`-ის row-ში ინახება (jsonb), მაგრამ შიგთავსი კვლავ `{id, bg}` mock ფორმატია, Storage-თან დაკავშირების გარეშე (ცალკე, სულ სხვა ეტაპია).
    - SQL + RLS: `reviews` — **SELECT საჯაროა ყველა ავტორიზებული მომხმარებლისთვის** (`to authenticated using (true)`, არა owner-only, როგორც სხვა ცხრილებში) — შეფასებები განზრახ საჯარო მონაცემია პროდუქტულად (სხვა Customer-ებმა უნდა ნახონ Provider-ის შეფასებები გადაწყვეტილების მისაღებად), INSERT კი მხოლოდ `customer_id = auth.uid()`-ს უშვებს.

**დაგეგმილი Supabase ცხრილების (#52-#58) სრული ციკლი დასრულებულია** — `users`, `provider_profiles`, `job_posts`, `job_responses`, `messages` (ტექსტი), `reviews`. მომხმარებელმა მოთხოვნა კიდევ სამი დამატებითი გაუმჯობესება, ნაბიჯ-ნაბიჯ: **ჩატის Realtime (#59, დასრულებული)**, **საჯარო Provider დირექტორია (#60, დასრულებული)**, **Storage (#61 "ეტაპი A" + #62 "ეტაპი B" — job post-ის ფოტოები, Provider-ის სერთიფიკატები/portfolio, RatingScreen-ის ფოტოები — ყველა რეალურია; მხოლოდ პროფილის ფოტო (`hasPhoto`) დარჩა, `Avatar`-ის ცალკე დიზაინის გადაწყვეტილების მოლოდინში)**. Customer-ის მხარეს job-ის ფოტოების ჩვენება (#63), rating/reviews აგრეგაცია (#64), Provider-ის პროფილის ფოტო (#65), ჩატის structured "offer" ტიპი (#66) და job-ის Provider-ზე რეალური მინიჭება + `jobs`-ის აგრეგაცია (#67) — ყველა დასრულებულია. **ცნობილი ეტაპები აღარ დარჩა** — იხ. #67-ის ბოლო აბზაცი დარჩენილი, განზრახ-გამარტივებული ნაწილებისთვის (`ProviderMyJobsScreen`, `conversations`).
59. **ჩატის Realtime — მეორე მხარის შეტყობინება მაშინვე ჩნდება, ეკრანის ხელახლა გახსნის გარეშე.** #57-ის "ეტაპი A"-ს პირდაპირი გაგრძელება, ცალკე სესიაში. `chatService.ts`-ს დაემატა `subscribeToMessages(customerId, providerId, myUid, onMessage)` — Supabase-ის `postgres_changes` არხი (`channel().on('postgres_changes', {event:'INSERT', table:'messages', filter: customer_id=eq...})`), აბრუნებს unsubscribe ფუნქციას.
    - **განზრახ ფილტრავს საკუთარ შეტყობინებებს** (`row.sender_id === myUid` → არ გამოიძახება callback) — საკუთარი გაგზავნილი შეტყობინება უკვე ლოკალურად emატება `sendMsg`-ში, optimistic-ად (`sendRealMessage`-ის პასუხიდან, real db id-ით); Realtime-ითაც რომ დამატებულიყო იგივე წესით, დუბლირებული ჩანაწერი გამოვიდოდა — ლოკალურ temp id-სა (`new-...`) და db-ის namdvili id-ს შორის ვერასდროს მოხდებოდა დამთხვევა dedupe-ისთვის.
    - **`ChatConversationScreen`-ს დაემატა ერთი ახალი `useEffect`**, არსებული fetch-effect-ის გვერდით — subscribe mount-ზე (`isReal`-ის შემთხვევაში), unsubscribe unmount-ზე. დანარჩენი 811-ხაზიანი ფაილი უცვლელია. ავტომატური ქვემოთ-სქროლვა უკვე მუშა იყო (`onContentSizeChange`), დამატებითი wiring არ დასჭირდა.
    - **native rebuild არ იყო საჭირო** — `@supabase/supabase-js`-ის realtime ნაწილი უკვე node_modules-შია (#52-დან), React Native-ის ჩაშენებულ `WebSocket`-ს იყენებს, ახალი native დამოკიდებულება არ დამატებულა.
    - **მოთხოვს Supabase-ის მხრიდან ერთ ცალკე ნაბიჯს** — `messages` ცხრილი ხელით უნდა დაემატოს `supabase_realtime` publication-ს (`alter publication supabase_realtime add table public.messages;`), თორემ INSERT-ები საერთოდ არ გავრცელდება.
    - **structured "offer" ტიპის შეტყობინება (D3, #2/#45) კვლავ არ არის დაკავშირებული** — ეს ცალკე, დარჩენილი ეტაპია (ტექსტურის გარდა, ChatMsg-ის `type:'offer'`-ის ველების — amount/comment/offerStatus — persistence საჭიროებს).
60. **საჯარო Provider დირექტორია — `listProviders`/`getProviderById`-ის mock-ის გვერდით რეალური Supabase queries.** ბოლო დარჩენილი დიდი ეტაპი (#48-დან არაერთხელ ხსენებული, #53/#56/#58-ის "დამოკიდებულია საჯარო დირექტორიაზე" შენიშვნების დახურვა). **მთავარი ტექნიკური გადაწყვეტილება — `provider_profiles`-ს დაემატა `first_name`/`last_name`, #53-ის თავდაპირველი "მხოლოდ users-ში" გადაწყვეტილების override** — მიზეზი: `users` ცხრილს ვერასდროს გავხსნით საჯარო SELECT-ისთვის (Customer-ების email/მისამართიც შეიცავს), `provider_profiles` კი არაფერს მგრძნობიარეს არ ინახავს, ამიტომ სახელის დუბლირება საშუალებას იძლევა დირექტორია `users`-თან join-ის/security-definer view-ის გარეშე ავაშენოთ — იგივე დენორმალიზაციის ლოგიკა, რაც #55/#56/#58-ში (`customer_name`/`provider_name`).
    - **`userService.ts`-ს ტიპი შეეცვალა:** `getProviderProfileRecord`/`upsertProviderProfileRecord` ახლა სრულ `ProviderProfile`-ს (firstName/lastName-იანად) იღებს/აბრუნებს, `ProviderProfileRecord` (`Omit<...,'firstName'|'lastName'>`) ტიპი მთლიანად მოცილებულია — არსად არ იყო საჯაროდ import-ილი, უსაფრთხოდ შეიცვალა. `ProviderSetupScreen`/`ProviderEditProfileScreen`-ის upsert-ის call-ებს დაემატა `firstName`/`lastName` (უკვე ხელთ არსებული local/context state-იდან).
    - **ახალი `listRealProviders(): Promise<Provider[]>`** — კითხულობს `provider_profiles`-ის ყველა row-ს (ახალი public-read policy) და თარგმნის მდიდარ `Provider` ტიპში. Real-only ცნობილი ველები: name/initials/color/bio/areas/location/specialties/certificates/portfolio/sqmPrice/years (experience id → წლების რიცხვი, ახალი `EXPERIENCE_YEARS` lookup). **rating/reviews/jobs/verified/online/price/category** ნაგულისხმევებზეა (`0`/`false`/`''`/პირველი specialty id) — ეს აგრეგირებული სტატისტიკა (`reviews`/`job_posts`-დან დათვლილი) ცალკე, მომავალი დახვეწაა; `isNewProvider`/`weightedRating` (#42/#43) უსაფრთხოდ ამუშავებენ ამ default-ებს "ახალი ოსტატის" ვიზუალურ მდგომარეობად.
    - **მიდგომა ეკრანებზე — "mock + real merge", არა "mock-ის ჩანაცვლება".** ოთხივე მომხმარებელი ეკრანი (`CustomerHomeScreen`, `CustomerCategoryScreen`, `SavedProvidersScreen`, `ViewProviderProfileScreen`) საწყისად კვლავ mock `PROVIDERS`-ს რენდერავს (სინქრონულად, ისე, როგორც აქამდე), მერე ახალი `useEffect` ფონურად კითხულობს `listRealProviders()`-ს და **ამატებს** (არა ცვლის) რეალურ Provider-ებს სიის ბოლოში (id-ით დუბლირების დაცვით). ეს, `job_posts`/`job_responses`-ის "მხოლოდ საკუთარი" სიებისგან განსხვავებით (სადაც mock სრულად ჩანაცვლდა), შეგნებული არჩევანია — Provider დირექტორია გაზიარებული/საჯარო კატალოგია, არა პერსონალური მონაცემი, ამიტომ mock დემო-ოსტატების და რეალურების თანაარსებობა პროდუქტულადაც სწორია და ტესტვისასაც არ აცარიელებს აპს ერთბაშად.
    - **`ViewProviderProfileScreen`-ის #58-ში დაფიქსირებული ბაგი გასწორდა.** მანამდე `route.params.id`-ის real UUID-ზე ეს ეკრანი მცდარად `listProviders()[0]`-ზე (პირველ mock Provider-ზე) vardebda. ახლა: `p` `const`-იდან `useState`-ად გადაკეთდა, `useEffect` მხოლოდ მაშინ ცდილობს `listRealProviders()`-ს, როცა mock-ში ვერაფერი მოიძებნა (`userService.getProviderById(id)` undefined) — თუ ნამდვილ დირექტორიაში მოიძებნა, `p`-ც და მისი `reviews`-იც (ახლა `reviewService.listRealReviewsForProvider`-ითაც, #58-ის ადრე გამორთული read-მხარეც აქ ბოლოს ჩაერთო) რეალურზე გადადის.
    - SQL + RLS: `provider_profiles`-ს დაემატა ახალი SELECT policy — `to authenticated using (true)` (საჯარო, ყველა ავტორიზებული მომხმარებლისთვის, `reviews`-ის (#58) იგივე პრინციპით) — ეს **ემატება** არსებულ owner-only policy-ს, არ ცვლის (INSERT/UPDATE კვლავ მხოლოდ owner-ისთვისაა).
61. **Storage — "ეტაპი A", job post-ის ფოტოების რეალური ატვირთვა Supabase Storage-ში.** პირველი ეტაპი, სადაც ამ სესიაში **ახალი native პაკეტი დაემატა** (`expo-image-picker`, SDK 57-თან თავსებადი ვერსია `npx expo install`-ით) — განსხვავებით #52-#60-ის ყველა დანარჩენი ცვლილებისგან (JS-ონლი, native rebuild-ის გარეშე), ეს ეტაპი **ნამდვილად საჭიროებს ახალ native rebuild-ს** (`app.json`-ს დაემატა `expo-image-picker` config plugin, camera/photos permission ტექსტებით).
    - **5 დაგეგმილი ატვირთვის ადგილიდან (job post, Provider-ის სერთიფიკატები/portfolio/პროფილის ფოტო, RatingScreen) მხოლოდ job post-ის ფოტოები დაუკავშირდა.** დანარჩენი 4 იზიარებს საერთო `MediaUploadGrid`/`MediaItem` (`{id, bg}`) კომპონენტს დეტერმინისტული ფერების ციკლით (#48-ის შენიშვნა — `nextMediaItem` განზრახ არ იყო storageService-თან დაკავშირებული) — ამ საერთო კომპონენტისა და ტიპის რეალურ ფოტოებზე გადაყვანა (MediaItem-ს `uri?: string` უნდა დაემატოს, `MediaUploadGrid`/`MediaPreviewModal`-ს რეალური `<Image>` რენდერი) ცალკე, "Storage ეტაპი B"-დაა დატოვებული — PostJobScreen-ს კი **არასდროს** ჰქონია ეს გაზიარებული კომპონენტი (`photos: number[]`-ის საკუთარი, დამოუკიდებელი მოჩვენებითი სისტემა), ამიტომ ყველაზე იზოლირებული, დაბალრისკიანი საწყისი წერტილი იყო.
    - **`storageService.ts`-ს დაემატა** `uploadJobPhoto(uid, localUri): Promise<string>` — ლოკალურ ფაილს (`fetch(uri).then(r=>r.blob())`, React Native-ის ჩაშენებული `fetch`/`Blob`-ის მეშვეობით, დამატებითი პაკეტის გარეშე) წერს `job-photos` bucket-ში (`{uid}/{timestamp}-{random}.{ext}` path-ით), აბრუნებს საჯარო URL-ს (`getPublicUrl`). არსებული `uploadMedia()`/`deleteMedia()` (mock, `MediaUploadGrid`-ისთვის) **უცვლელია**.
    - **`PostJobScreen`-ის ფოტოს სექცია მთლიანად რეალურია** — `photos: number[]` (fake id-ები) → `photos: string[]` (ლოკალური URI-ები), ორი ცალკე ღილაკი (`pickFromCamera`/`pickFromGallery`, `ImagePicker.launchCameraAsync`/`launchImageLibraryAsync`, permission request-ით და შესაბამისი ქართული შეცდომის ტექსტით უარის შემთხვევაში) ცვლის ერთიან fake `addPhoto`-ს (რომელიც ორივე ღილაკზე იდენტურ მოჩვენებით ფერს ამატებდა). ბადეში ახლა რეალური `<Image source={{uri}}>` thumbnail ჩანს, ფერადი placeholder-ის ნაცვლად. "გამოქვეყნება"-ზე დაჭერისას ყველა ლოკალური ფოტო პარალელურად იტვირთება (`Promise.all`) და მიღებული URL-ების მასივი `job_posts.photos`-ში იწერება.
    - **`jobService.ts`-ს** `JobPostRow`/`NewJobPostInput`-ს დაემატა `photos: string[]` — `createCustomerJob` წერს, `fromJobPostRowToFeedJob`-ის `hasPhoto` ახლა **რეალურად** გამოითვლება (`row.photos.length > 0`, ადრე ყოველთვის `false` იყო, #55-ის ცნობილი გამარტივება) — Provider-ის Job Feed-ის "ფოტოა" tag ახლა სწორია. `fromJobPostRow` (CustomerJob-ისთვის) **არ კითხულობს/აბრუნებს photos-ს** — `CustomerJob` ტიპს/`CustomerJobDetailScreen`-ს ფოტოს საჩვენებელი UI საერთოდ არ აქვს (განზრახ, ცალკე scope — ატვირთვა/შენახვა უკვე რეალურია, ჩვენება Customer-ის მხარეს შემდეგი ბუნებრივი ნაბიჯია).
    - **მოთხოვს Supabase-ის მხრიდან ორ ცალკე ნაბიჯს:** (1) `job-photos` **public** bucket-ის შექმნა (Storage → New bucket, "Public bucket" ჩართული) — Dashboard-ში, არა SQL-ით; (2) storage policy INSERT-ისთვის (`storage.objects`-ზე, `bucket_id = 'job-photos'`, `to authenticated`) — public bucket-ის საკითხავი მხარე ავტომატურად საჯაროა, ცალკე SELECT policy არ სჭირდება.
    - SQL: `job_posts`-ს დაემატა `photos` სვეტი (`text[] not null default '{}'`).
62. **Storage — "ეტაპი B", Provider-ის სერთიფიკატები/ნამუშევრები და RatingScreen-ის დასრულების ფოტოები.** #61-ის პირდაპირი გაგრძელება, იმავე სესიაში. **native rebuild ამ ეტაპზე აღარ დასჭირდა** — `expo-image-picker` უკვე კომპილირებული იყო #61-იდან.
    - **`MediaItem` ტიპს (`MediaUploadGrid.tsx`) დაემატა არასავალდებულო `uri?: string`** — თუ არსებობს, `MediaUploadGrid`-იც და `MediaPreviewModal`-იც რეალურ `<Image source={{uri}}>`-ს რენდერავენ ფერადი placeholder-ის ნაცვლად (fallback უცვლელია, `uri`-ს არარსებობისას). ეს ერთი ტიპის/ორი კომპონენტის ცვლილება ავტომატურად სარგებელს აძლევს სამივე მომხმარებელს (სერთიფიკატები, portfolio, RatingScreen), რადგან ერთსა და იმავე გაზიარებულ კომპონენტს იზიარებენ.
    - **`MediaUploadGrid`-ის props შეიცვალა — ერთი `onAdd`-ის ნაცვლად ორი ცალკე `onAddCamera`/`onAddGallery`.** ადრე ორივე ვიზუალურად განსხვავებული ღილაკი ("გადაღება"/"გალერეა") იმავე fake `onAdd`-ს იძახებდა (მოჩვენებითი განსხვავება, mock-ის დროს არავითარი პრაქტიკული მნიშვნელობა არ ჰქონდა) — რეალურ picker-ზე გადასვლისას ეს განსხვავება რეალურად საჭირო გახდა (კამერა vs გალერეა სხვადასხვა `expo-image-picker` API-ს იძახებს). ეს არის ერთადერთი "breaking" props ცვლილება ამ სესიაში — ყველა 5 გამომძახებელი (Provider-ის 2 ეკრანი × 2 ბადე + RatingScreen-ის 1) განახლდა ერთდროულად.
    - **პროფილის ფოტო (`hasPhoto: boolean`) განზრახ არ არის ამ ეტაპში** — `Avatar` კომპონენტი მთელ აპში მხოლოდ ინიციალებზეა აგებული (`initials`/`color` props, `uri`/photo-ს მხარდაჭერის გარეშე ყველგან — ჩატი, პროფილები, provider ბარათები), ამ პატერნის რეალურ ფოტოზე გადაყვანა (`Avatar`-ის თავად კომპონენტის გაფართოება + ყველა გამომძახებლის გადახედვა) ცალკე, გაცილებით დიდი გადაწყვეტილებაა, ვიდრე უკვე array-based `MediaItem[]`-ზე აგებული certificates/portfolio/rating-photos.
    - **`storageService.ts`-ს დაემატა** `uploadUserMedia(uid, localUri, kind: 'certificate'|'portfolio'|'rating'): Promise<string>` — საერთო `user-media` bucket, `kind`-ით ცალკე "საქაღალდით" (`{kind}/{uid}/...`) — ერთი ახალი bucket/policy სამივე მომხმარებლისთვის, `job-photos`-ის (#61) ცალკე bucket-ის ნაცვლად, რომ მომხმარებელს რაც შეიძლება ნაკლები Dashboard-ნაბიჯი დასჭირდეს.
    - **"ატვირთვა შენახვისას", არა "არჩევისთანავე"** — იმავე პრინციპით, რაც job post-ის ფოტოებს ჰქონდა (#61): ფოტოს არჩევისას მხოლოდ ლოკალური URI ემატება state-ს (მყისიერი, უფასო preview), რეალური Supabase-ის ატვირთვა ხდება მხოლოდ საბოლოო შენახვაზე დაჭერისას (`ProviderSetupScreen`-ის "პროფილის შექმნა", `ProviderEditProfileScreen`-ის "ცვლილებების შენახვა" — მეორე, წარმატებულ მცდელობაზე, #53-ის დემო-ქცევის უცვლელად, `RatingScreen`-ის "შეფასების გაგზავნა"). ახალი `uploadPendingMedia`/`ProviderEditProfileScreen`-ის ანალოგიური helper-ი **მხოლოდ ლოკალურ (non-http) `uri`-ებს ატვირთავს** — `ProviderEditProfileScreen`-ზე უკვე შენახული (http URL-იანი) სერთიფიკატები/portfolio ხელახლა არ იტვირთება რედაქტირებისას.
    - **`RatingScreen`-ის `handleSubmit` სინქრონულიდან ასინქრონურ გახდა** — ახალი `submitting` state + `Button`-ის `loading`/`loadingLabel` (ატვირთვის დროს ღილაკი დაბლოკილია). ატვირთვის ჩავარდნისას შეფასება მაინც იგზავნება (ლოკალური, ატვირთვამდელი `uri`-ებით) — მომხმარებლის სავალდებულო შეფასების flow-ს (#6/#19) ფოტოს ატვირთვის ხარვეზი არ უნდა აჩერებდეს.
    - **მოთხოვს Supabase-ის მხრიდან ორ ცალკე ნაბიჯს:** (1) `user-media` **public** bucket-ის შექმნა (Storage → New bucket); (2) storage policy INSERT-ისთვის (`bucket_id = 'user-media'`, `to authenticated`) — #61-ის `job-photos`-ის იგივე pattern-ით, ცალკე ცალკე bucket-ისთვის.
63. **Customer-ის მხარეს job-ის ფოტოების ჩვენება — #61-ის ბოლო დარჩენილი ნაწილი.** ფოტოები უკვე იწერებოდა `job_posts.photos`-ში (#61), მაგრამ `CustomerJobDetailScreen` მას საერთოდ არ კითხულობდა — ამის მაგივრად job-ის აღწერის ქვემოთ **ყოველთვის 2 ჰარდქოდილი ფერადი placeholder** ჩანდა (`job.id !== 'j3'` mock-id-ზე მიბმული პირობით). ეს ბაგისმაგვარი, სუფთა mock ნარჩენი იყო — არც ერთ რეალურ job-ს არ ჰქონდა კავშირი ამ ვიზუალთან.
    - **`CustomerJob` ტიპს დაემატა `photos?: string[]`**, `jobService.ts`-ის `fromJobPostRow` ახლა კითხულობს `row.photos`-ს (მანამდე ცნობილად გამოტოვებული, #61-ის შენიშვნა).
    - **სამნაირი ჩვენება, ერთმანეთის გამომრიცხავი:** `job.photos` **განსაზღვრული და არა-ცარიელია** → რეალური `<Image>` thumbnail-ები საჯარო URL-ებით; `job.photos` **განსაზღვრული, მაგრამ ცარიელია** (რეალური job, ფოტოს გარეშე) → სექცია საერთოდ არ ჩანს; `job.photos` **საერთოდ არ არსებობს** (`undefined` — ძველი mock demo j1/j2/j3, რომლებსაც ეს ველი არასდროს ჰქონია) → ძველი ჰარდქოდილი 2-placeholder-იანი ჩვენება უცვლელად რჩება, ისე რომ დემო-ეკრანები არ დაზიანდეს.
    - **არც SQL, არც native rebuild არ დასჭირდა** — მონაცემი უკვე არსებობდა ბაზაში, მხოლოდ UI-ს აკლდა კავშირი.
64. **Provider დირექტორიის რეიტინგის/შეფასების რაოდენობის რეალური აგრეგაცია — `jobs` (დასრულებული სამუშაოები) გამონაკლისით.** #60-ის `listRealProviders()`-ს `rating`/`reviews` ველები ნაგულისხმევებზე ჰქონდა (`0`/`0`) — ეს ეტაპი მათ რეალურ `reviews`-ის მონაცემებზე აგებს.
    - **ახალი Postgres VIEW `provider_stats`** (`reviews`-იდან `group by provider_id`, `avg(stars)`/`count(*)`) — SELECT ცალკე query-ით ერთდროულად (`Promise.all`) `provider_profiles`-თან ერთად, `Map<provider_id, stats>`-ად აწყობილი და თითო Provider-ს დაერთვის `id`-ის მიხედვით.
    - **`jobs` (დასრულებული სამუშაოების რაოდენობა) განზრახ დარჩა `0`-ზე** — ამის რეალური დათვლა job-ის Provider-ზე "მინიჭებას" საჭიროებს (#47-ის ორმხრივი დასრულების state machine — `awaiting_customer_confirmation`→`completed`), რომელიც ჯერ კიდევ მხოლოდ ლოკალურ `JobStatusContext`-შია, არა Supabase-ზე — `job_responses`-ს (#56) არ აქვს "არჩეულია"/"დასრულებულია" კონცეფცია, მხოლოდ "დაინტერესებულია". ეს ცალკე, უფრო დიდი ეტაპია (job_posts-ს დასჭირდება provider_id/status-columns-ის გაფართოება).
    - **error-tolerant** — `provider_stats`-ის query-ის შეცდომა (მაგ. SQL ჯერ არ გაშვებულა) არ ბლოკავს `listRealProviders()`-ს მთლიანად — უბრალოდ ყველა Provider-ს `rating: 0, reviews: 0` რჩება ("ახალი ოსტატი" ბეჯი), `provider_profiles`-ის ძირითადი query-ის შედეგი მაინც ბრუნდება.
    - **ეკრანების მხარეს ცვლილება არ დასჭირდა** — #60-ში აშენებული "mock + real merge" (`CustomerHomeScreen`/`CustomerCategoryScreen`/`SavedProvidersScreen`/`ViewProviderProfileScreen`) ავტომატურად სარგებლობს ამ გაუმჯობესებით, რადგან ყველა `listRealProviders()`-ის იმავე პასუხს იყენებს.
    - SQL:
      ```sql
      create view public.provider_stats as
      select
        provider_id,
        round(avg(stars)::numeric, 1) as avg_rating,
        count(*) as review_count
      from public.reviews
      group by provider_id;

      grant select on public.provider_stats to authenticated;
      ```
65. **Provider-ის პროფილის ფოტოს რეალური ატვირთვა — `Avatar` კომპონენტს დაემატა `uri` მხარდაჭერა.** ბოლო დარჩენილი "ცნობილი" ეტაპი #61-#62-ის Storage-მუშაობიდან — მანამდე პროფილის ფოტო (`hasPhoto: boolean`) ლოკალური, უშედეგო ტოგლი იყო.
    - **`ProviderProfile.hasPhoto: boolean` მთლიანად ჩანაცვლდა `photoUrl?: string`-ით** (ისევე `Provider`-საც დაემატა იგივე ველი) — ლოკალური boolean-ი აზრს კარგავს, როცა URL-ის არსებობა თავად უკვე გვეუბნება ფოტო აქვს თუ არა. `ProviderProfileContext.tsx`-ის `computeCompleteness` განახლდა (`!!p.photoUrl`, ადრინდელი `p.hasPhoto`-ს ნაცვლად) — პროცენტული ლოგიკა უცვლელია.
    - **`Avatar.tsx`-ს დაემატა არასავალდებულო `uri?: string` prop** — თუ არსებობს, რეალურ `<Image>`-ს რენდერავს წრეში, ინიციალების ნაცვლად (fallback ინიციალებზე, `uri`-ს არარსებობისას, უცვლელია). ეს ერთი, პატარა ცვლილება ავტომატურად ვრცელდება ყველგან, სადაც `Avatar` უკვე გამოიყენება.
    - **`storageService.ts`-ს `UserMediaKind`-ს დაემატა `'profile'`** — იმავე `user-media` bucket-ს იზიარებს, რაც სერთიფიკატები/portfolio/rating (#62), ახალი bucket არ დასჭირდა.
    - **`ProviderSetupScreen`/`ProviderEditProfileScreen`-ის ატვირთვა-შენახვისას პატერნი** (#62-ის იგივე პრინციპი) — ლოკალური URI არჩევისთანავე, საბოლოო ატვირთვა `handleContinue`/`handleSave`-ზე. `ProviderSetupScreen`-ს ადრე ჰქონდა ორი ცალკე ღილაკი ("ფოტოს გადაღება"/"გალერეიდან არჩევა") ორივე ერთსა და იმავე fake `setHasPhoto(true)`-ს იძახებდა — ახლა რეალურად ცალკეა (`pickProfilePhoto('camera'|'gallery')`).
    - **6 დამატებითი ეკრანი განახლდა** `uri` prop-ის გადასაცემად: `ProviderProfileScreen` (Provider-ის საკუთარი), `ViewProviderProfileScreen`, `CustomerHomeScreen` (ტოპ Provider-ების ბარათი), `CustomerCategoryScreen`, `SavedProvidersScreen` — ყველა უკვე #60-ის `listRealProviders()`-ის ბოლოშივე იღებდა Provider ობიექტს, უბრალოდ `photoUrl`-ს არ ავრცელებდა Avatar-მდე.
    - **mock `PROVIDERS` (`mockHomeData.ts`) დაუხებელი დარჩა** — `photoUrl` არასავალდებულოა, mock-ჩანაწერებზე `undefined` რჩება, ვიზუალურად უცვლელი (ინიციალები).
    - SQL: `provider_profiles`-ს დაემატა `photo_url text` სვეტი (ძველი `has_photo` boolean სვეტი გამოუყენებელი რჩება ბაზაში, არ წაშლილა — უვნებელია).
66. **ჩატის სტრუქტურირებული ფასის შეთავაზება (D3, #2/#45) — ბოლო "ცნობილი, ჯერ არ დაწყებული" ეტაპი.** #57/#59-ის `messages` ცხრილი მანამდე მხოლოდ ტექსტს იჭერდა (`ChatMsg`-ის `type:'offer'` ველები — `amount`/`comment`/`offerStatus` — არასდროს იწერებოდა, მხოლოდ ლოკალურ state-ში ცხოვრობდა). ორივე მიმართულება (Provider სთავაზობს, Customer ეთანხმება/უარყოფს) ახლა რეალურია.
    - **`messages` ცხრილს დაემატა 4 სვეტი:** `type` ('text'/'offer', default 'text'), `amount`, `comment`, `offer_status` — ტექსტური შეტყობინებები უცვლელად მუშაობს (`type='text'`-ით), offer-ები იმავე ცხრილშივეა, ცალკე ცხრილის გარეშე.
    - **ახალი RLS policy საჭირო გახდა — `respondToRealOffer`-ს UPDATE სჭირდება, რაც #57-ს არ ჰქონდა** (მხოლოდ SELECT/INSERT policy-ები არსებობდა) — ეს ცნობილი, გამოსწორებული ხარვეზი იყო: RLS-ის default-deny-ის გამო, ფასზე პასუხის UPDATE აქამდე უბრალოდ ჩავარდებოდა (თუმცა ეს არასდროს არ შემოწმებულა რეალურად, რადგან offer-ის UI ამ სესიამდე საერთოდ mock-ზე იყო).
    - **`chatService.ts`-ს დაემატა** `sendRealOffer(customerId, providerId, senderId, amount, comment?)` და `respondToRealOffer(messageId, 'accepted'|'declined')`. `subscribeToMessages` (#59) გაფართოვდა `event: 'INSERT'`-იდან `event: '*'`-მდე — ახლა UPDATE-ებსაც (ანუ ფასის დათანხმება/უარყოფა) აწვდის callback-ს, საკუთარი (sender_id===myUid) მხოლოდ ახალ INSERT-ებზეა გაფილტრული (დუბლირების თავიდან ასაცილებლად, #59-ის იგივე მიზეზი) — UPDATE-ები ორივე მხარეს მიდის ყოველთვის (idempotent replace).
    - **`ChatConversationScreen`-ის realtime merge-ლოგიკა "skip if exists"-იდან "replace if exists, else append"-ზე შეიცვალა** — ეს საჭირო გახდა სწორედ UPDATE-ების (offer-ის სტატუსის ცვლილების) მხარდასაჭერად; ახალი შეტყობინებებისთვის ქცევა უცვლელია.
    - **`sendOffer`/`respondToOffer`** (ორივე screen-ში) — არსებული ლოკალური optimistic-append/state-ცვლილება უცვლელია, დამატებით `isReal`-ზე fire-and-forget-ად წერენ Supabase-ში (იგივე pattern, რაც `sendMsg`-ს).
    - **დათანხმებული ფასის job-ის ჩანაწერში (`job_posts`) შენახვა კვლავ არ არის დაკავშირებული** — job_posts-ს ჯერ არ აქვს "დათანხმებული ფასის" ველი; ეს ცალკე, უფრო დიდი ცვლილებაა (# 2-ის თავდაპირველი TODO კომენტარიდანვე ცნობილი შეზღუდვა, ახლაც ასეთივე რჩება — მხოლოდ ჩატის შეტყობინების დონეზეა რეალური).
    - SQL:
      ```sql
      alter table public.messages add column type text not null default 'text';
      alter table public.messages add column amount numeric;
      alter table public.messages add column comment text;
      alter table public.messages add column offer_status text;

      create policy "Participant can update messages"
        on public.messages for update
        using (auth.uid() = customer_id or auth.uid() = provider_id);
      ```
67. **Job-ის Provider-ზე რეალური "მინიჭება" + `Provider.jobs` (დასრულებული სამუშაოების რაოდენობა) რეალური აგრეგაცია — ბოლო დარჩენილი ცნობილი ეტაპი, ამოწურულია.** #47-ის ორმხრივი დასრულების state machine (`JobStatusContext`) აქამდე მთლიანად ლოკალური, in-memory იყო — `job_posts`-ს არც `provider_id` ჰქონდა, არც სტატუსის ცვლილება ვრცელდებოდა Supabase-ში.
    - **`job_posts`-ს დაემატა `provider_id uuid`** — Customer-ის მიერ Provider-ის არჩევისას (`CustomerJobDetailScreen`-ის `confirmSelection`) ივსება.
    - **`CustomerJob.status`-ის ტიპი გაფართოვდა** — ადრე ვიწრო `'active'|'pending'|'completed'|'cancelled'` იყო, `JobStatusContext`-ის რეალურად უფრო ფართო მდგომარეობებთან (`awaiting_customer_confirmation`/`disputed`) შეუსაბამოდ; ახლა პირდაპირ `JobStatus`-ს იზიარებს (ტიპის დუბლირების გარეშე).
    - **`JobStatusContext`-ის `setStatus`-ს დაემატა არასავალდებულო მესამე პარამეტრი, `providerId`** — ლოკალური, სინქრონული Record-ის ცვლილება (ყველა არსებული გამომძახებელი) **მთლიანად უცვლელია**; დამატებით, ცენტრალიზებულად (ერთ ადგილას, ოთხივე გამომძახებლის ნაცვლად ცალ-ცალკე დუბლირების მაგივრად) `isUuid(jobId)`-ის შემთხვევაში ფონურად წერს `jobService.updateJobStatus(jobId, status, providerId)`-ს. `CustomerJobDetailScreen`-ის `confirmSelection`-მა `setStatus(job.id, 'active')`-ს დაამატა `confirmProvider.id` — ეს ერთადერთი call site, რომელსაც `providerId` ესაჭიროებოდა (დანარჩენ 3 გადასვლას job უკვე მინიჭებული ჰყავს).
    - **ახალი RLS policy საჭირო გახდა** — `awaiting_customer_confirmation`-ზე გადასვლას (`ProviderJobDetailScreen`-ის "სამუშაო დავასრულე") **Provider** წერს, არა job-ის owner-Customer, რასაც #54-ის "Customer can update own jobs" policy არ ფარავდა.
    - **`provider_stats` view (#64) გაფართოვდა** — `reviews`-ზე დაფუძნებული `avg_rating`/`review_count`-ის გვერდით ახლა `job_posts`-იდან (`status='completed'` count) დაითვლება `completed_jobs`-იც. View-ის საწყისი structure (`group by` პირდაპირ `reviews`-ზე) აღარ იყო საკმარისი ორი წყაროს გასაერთიანებლად — `DROP`/`CREATE`-ით თავიდან აშენდა, `provider_profiles`-ზე `left join`-ებით (რომ Provider-ები 0 შეფასებით/0 job-ით არ დაიკარგონ inner join-ის გამო).
    - **`userService.ts`-ის `ProviderStatsRow`-ს დაემატა `completed_jobs`**, `fromProviderProfileRowToPublicProvider`-ის `jobs: stats?.completed_jobs ?? 0` (ადრე მუდმივად `0` იყო ჰარდქოდილი).
    - SQL:
      ```sql
      alter table public.job_posts add column provider_id uuid references auth.users(id);

      create policy "Assigned provider can update job status"
        on public.job_posts for update
        using (provider_id = auth.uid());

      drop view if exists public.provider_stats;

      create view public.provider_stats as
      select
        pp.id as provider_id,
        coalesce(r.avg_rating, 0) as avg_rating,
        coalesce(r.review_count, 0) as review_count,
        coalesce(j.completed_jobs, 0) as completed_jobs
      from public.provider_profiles pp
      left join (
        select provider_id, round(avg(stars)::numeric, 1) as avg_rating, count(*) as review_count
        from public.reviews group by provider_id
      ) r on r.provider_id = pp.id
      left join (
        select provider_id, count(*) as completed_jobs
        from public.job_posts where status = 'completed' group by provider_id
      ) j on j.provider_id = pp.id;

      grant select on public.provider_stats to authenticated;
      ```
68. **ჩატების სია (D1) რეალურია + ჩატის ფოტოს ატვირთვა რეალურია — მომხმარებლის მოთხოვნით დაწყებული "ბოლო fake-გასუფთავების" ეტაპის პირველი ორი პუნქტი.** #57-ში შეგნებულად არჩეული იყო `conversations`-ის ცალკე ცხრილის გარეშე დარჩენა (chatId = "მეორე მხარის id", `messages`-ზე პირდაპირ query) — ეს საკმარისი იყო ერთი კონკრეტული ჩატის გახსნისთვის, მაგრამ "ჩემი ყველა ჩატის სია, ბოლო შეტყობინებით და წაუკითხავის counter-ით" PostgREST-ის უბრალო query-ით ვერ გამოითვლება (Realtime-ის `filter` მხოლოდ ერთი სვეტის ტოლობას უჭერს მხარს, `customer_id=me OR provider_id=me` ერთ filter string-ში არ გამოისახება) — ამიტომ ახალი **დენორმალიზებული** `conversations` ცხრილი დაემატა, `messages`-ის ყოველი გაგზავნის გვერდითი ეფექტით (`touchConversation`) განახლებადი.
    - **`chatService.ts`-ს დაემატა** `sendRealImage`, `listMyConversations(myUid, myRole)`, `markConversationRead`, `subscribeToUnreadCount` — არსებული `listChats`/`getChatById`/`getMessages` (mock) **უცვლელია**. `ChatParticipants` ტიპი (customer/provider-ის name/initials/color) ახლა `sendRealMessage`/`sendRealOffer`/`sendRealImage`-ის სავალდებულო არგუმენტია — `touchConversation`-ს სჭირდება ორივე მხარის საჩვენებელი ინფო, `conversations`-ის row-ის შესაქმნელად/განსაახლებლად.
    - **`ChatsListScreen`** აღარ იყენებს mock `listChats()`-ს — `listMyConversations(uid, role)`, `useFocusEffect`-ით (Bottom Tab-ის ეკრანი session-ის განმავლობაში მონტირებული რჩება, #54-ის იგივე პრინციპი). რეალურ `ChatEntry`-ებს job-ის ველები (`jobTitle`/`jobCategory`/...) არ აქვთ — `conversations` ცხრილს job-თან კავშირი ჯერ არ აქვს, ჩატის job-ბანერი მხოლოდ mock chat-ებზე ჩანს ჯერჯერობით (ცნობილი, დარჩენილი შეზღუდვა).
    - **ჩატის ფოტოები რეალურია** — `ChatConversationScreen`-ის `sendImg` (fake ფერადი placeholder) ჩანაცვლდა `pickAndSendImage('camera'|'library')`-ით, #61/#62-ის იგივე `expo-image-picker` → `storageService.uploadUserMedia(uid, uri, 'chat')` პატერნით — `user-media` bucket-ი და მისი policy უკვე არსებობდა (#62), ახალი bucket/Dashboard-ნაბიჯი არ დასჭირდა. `ChatMsg`-ს დაემატა `imageUrl?: string` (`imgColor`-ის გვერდით, ძველი mock-სურათების ვიზუალურად უცვლელად დატოვებისთვის) — სურათის ბუშტიც, სრულ ეკრანზე გახსნის preview-იც ახლა რეალურ `<Image>`-ს რენდერავს, როცა `imageUrl` არსებობს.
    - **Tab-bar-ის წაუკითხავი-ჩატის წითელი წერტილი (`CustomerTabs`/`ProviderTabs`) რეალური და ცოცხალი გახდა** — ადრე module-level `const unreadChats = chatService.listChats()...` იყო (გამოთვლილი მხოლოდ import-ის დროს, არასდროს არ იცვლებოდა, mock-ზეც კი). ახლა `subscribeToUnreadCount(uid, role, callback)` — mount-ზე ერთხელ ითვლის და Realtime-ით (`conversations`-ის ცვლილებაზე) ცოცხლად ანახლებს.
    - **მოთხოვს Supabase-ის მხრიდან ერთ ცალკე ნაბიჯს** — `conversations` ცხრილიც `supabase_realtime` publication-ს უნდა დაემატოს (`messages`-ის, #59-ის იგივე პრინციპით), თორემ tab-ის badge ცოცხლად არ განახლდება.
    - SQL:
      ```sql
      alter table public.messages add column image_url text;

      create table public.conversations (
        customer_id uuid not null references auth.users(id),
        provider_id uuid not null references auth.users(id),
        customer_name text not null,
        customer_initials text not null,
        customer_color text not null,
        provider_name text not null,
        provider_initials text not null,
        provider_color text not null,
        last_message text not null,
        last_message_at timestamptz not null default now(),
        customer_unread int not null default 0,
        provider_unread int not null default 0,
        primary key (customer_id, provider_id)
      );

      alter table public.conversations enable row level security;

      create policy "Participant can read conversations"
        on public.conversations for select
        using (auth.uid() = customer_id or auth.uid() = provider_id);

      create policy "Participant can insert conversations"
        on public.conversations for insert
        with check (auth.uid() = customer_id or auth.uid() = provider_id);

      create policy "Participant can update conversations"
        on public.conversations for update
        using (auth.uid() = customer_id or auth.uid() = provider_id);

      alter publication supabase_realtime add table public.conversations;
      ```
69. **"მიმდინარე სამუშაო" ბარათები (Customer/Provider Home) + Provider-ის "ჩემი სამუშაოები"/"შესრულებული სამუშაოები" — სამივე რეალურ `job_posts`-ზეა აგებული.** #68-ის გაგრძელება, "fake-გასუფთავების" ეტაპის მე-3/4/5 პუნქტები.
    - **`job_posts`-ს დაემატა `provider_name` სვეტი** (`customer_name`-ის, `job_responses.provider_name`-ის იგივე დენორმალიზაციის პრინციპი, #55/#56) — Customer-ის "ჩემი job"-ის კითხვისას `provider_profiles`-ის RLS-ის (owner-only) გვერდის ავლა არ სჭირდება, პირდაპირ `job_posts.provider`-ში ჩანს არჩეული Provider-ის სახელი. **`fromJobPostRow`-ს `provider: null`-ის ჰარდქოდი წაშლილია** — ეს ძველი, #54-დანვე ცნობილი გამარტივება იყო.
    - **`JobStatusContext.setStatus`-ს დაემატა მეოთხე არასავალდებულო არგუმენტი, `providerName`** — `jobService.updateJobStatus`-ს გადაეცემა, `provider_id`-ის გვერდით `provider_name`-საც წერს. ერთადერთი call site, რომელსაც სჭირდება: `CustomerJobDetailScreen`-ის `confirmSelection` (`setStatus(job.id, 'active', confirmProvider.id, confirmProvider.name)`).
    - **`jobService.ts`-ს დაემატა `listMyAssignedJobs(providerId): Promise<FeedJob[]>`** (`job_posts.provider_id = me`, ნებისმიერი სტატუსით) — სამივე ახალი real-data წყაროს საერთო საფუძველი. `FeedJob`-ს დაემატა არასავალდებულო `status?: JobStatus` (მხოლოდ ამ query-ის შედეგზეა შევსებული — Job Feed-ის ღია, ყოველთვის `'pending'` ჩანაწერებზე `undefined` რჩება). `fromJobPostRowToFeedJob`-ის `assignedProviderId: null` ჰარდქოდი შეიცვალა რეალურ `row.provider_id`-ზე.
    - **`ProviderHomeScreen`-ის "მიმდინარე სამუშაო"** — mock `PROVIDER_FEED`/`CURRENT_PROVIDER_ID` მექანიზმი (#30/#47) ჩანაცვლდა `listMyAssignedJobs(uid)`-ით (იმავე `useFocusEffect`-ში, feed/interests-თან ერთად); candidate — პირველი assigned job, რომლის ცოცხალი სტატუსიც (`getStatus(customerJobId) ?? job.status`) არც `completed` და არც `cancelled` არაა.
    - **`CustomerHomeScreen`-ის "მიმდინარე სამუშაო"** — `jobService.listCustomerJobs()` (mock, მუდმივად) ჩანაცვლდა `listMyJobPosts(uid)`-ის რეალურ fetch-ით, **მხოლოდ არა-ცარიელ პასუხზე** (#56-ის იგივე fallback-chain პრინციპი — mock demo j1/j2/j3 დაცულია ცარიელი ანგარიშისთვის).
    - **`ProviderMyJobsScreen` მთლიანად გადაწერილია** — ძველი `MyJobRow` (`{title, customer, addr, when, pay}`, id-ის გარეშე, #30-ის ცნობილი "ცალკე კუნძული" შეზღუდვა) `listMyAssignedJobs`-ის რეალურ `FeedJob[]`-ზეა შეცვლილი, `CustomerJobsScreen`-ის იგივე `useFocusEffect`+ტაბ-ფილტრის ნიმუშით. **დეკორატიული "დასრულება" ღილაკი (`onPress`-ის გარეშე, არასდროს არაფერს არ აკეთებდა) მთლიანად ამოღებულია** — რეალური დასრულების flow უკვე `ProviderJobDetailScreen`-შია (#47), ბარათზე დაჭერა იქ გადადის. `pay` ველი აღარ ჩანს — job_posts-ს ფასის ველი არ აქვს (#35-ის განზრახული შეზღუდვა).
    - **`ProviderCompletedJobsScreen` მთლიანად გადაწერილია** — ძველი mock `PROVIDER_COMPLETED_JOBS` (`reviewService.getCompletedJobs()`) ჩანაცვლდა `listMyAssignedJobs(uid)`-ის `status === 'completed'`-ზე გაფილტვრით + ახალი `reviewService.listReviewStarsByJob(uid)` (job_id→stars mapping, `reviews`-იდან) — `Review` ტიპს (საჯარო რევიუების სია) `job_id` არ აქვს, ამიტომ ცალკე მცირე query დასჭირდა job-სპეციფიური ვარსკვლავისთვის.
    - **mock methods (`listCustomerJobs`/`listProviderFeed`/`getOpenProviderFeed`/`getFeedJobById`/`listMyActiveJobs`/`listMyDoneJobs`/`getCompletedJobs`) და მათი მონაცემები (`mockHomeData.ts`/`mockReviews.ts`) უცვლელად რჩება ამ ეტაპზე** — საბოლოო წაშლის ერთიან, ბოლო ეტაპშია დაგეგმილი (მომხმარებლის მოთხოვნა), არა ეტაპობრივად.
    - SQL:
      ```sql
      alter table public.job_posts add column provider_name text;
      ```
70. **შეტყობინებების სისტემა (Notifications) — რეალური, ბოლო "ავაშენოთ" პუნქტი "fake-გასუფთავების" ეტაპიდან.** ახალი `notifications` ცხრილი, **client-side ჩანაწერით** (არა DB trigger) — თითო რეალურ მოვლენას (ახალი ჩატის შეტყობინება, ახალი დაინტერესება, job-ის სტატუსის ცვლილება) თან ახლავს `notificationService.create(recipientId, ...)` გამოძახება, ზუსტად `touchConversation`-ის (#68) იგივე "გვერდითი ეფექტის" პრინციპით — fire-and-forget, მთავარ მოქმედებას არასდროს არ ბლოკავს.
    - **ტიპები გატანილია ცალკე `src/types/notification.ts`-ში** (`NotificationEntry`/`NotificationTarget`) — დომეინის მოდელების რეფაქტორინგის (#48) იგივე პრინციპი. `role` ველი წაშლილია საზიარო ტიპიდან (რეალურ notifications-ს `user_id`-ით scoping ჰყავს, role აღარ სჭირდება) — `mockNotifications.ts`-ში ცალკე `MockNotificationEntry = NotificationEntry & { role: Role }` დარჩა mock-მასივის (`NOTIFICATIONS`) ტიპად, რომელიც role-ით ფილტრავს.
    - **ახალი `notificationService.ts`** — `listMine`/`markRead`/`markAllRead`/`create`/`subscribeToUnreadCount` (ბოლო ორი `chatService.subscribeToUnreadCount`-ის, #68, ზუსტად იგივე Realtime-count პატერნი).
    - **სამი წყარო, სადაც notification იქმნება:**
      1. **ახალი ჩატის შეტყობინება** — `chatService.ts`-ის ახალი `notifySender()` helper, სამივე `sendReal*`-ის (text/offer/image) ბოლოს გამოძახებული — მიმღები ყოველთვის "მეორე მხარეა" (`touchConversation`-ის იგივე `customerId===senderId` შემოწმებით), target `ChatConversation`-ია, `chatId`=sender-ის id (route-ის იგივე კონვენცია).
      2. **ახალი დაინტერესება** — `quoteService.expressInterest`-ს დაემატა არასავალდებულო მეოთხე არგუმენტი, `customerId` (job-ის owner) — თუ მოცემულია, insert-ის შემდეგ ქმნის Customer-ისთვის შეტყობინებას. ამის ამოქმედებას დასჭირდა **`FeedJob`-ს ახალი `customerId?: string`** (`fromJobPostRowToFeedJob`-ში `row.customer_id`-იდან შევსებული) — 3-ივე გამომძახებელი (`ProviderHomeScreen`/`ProviderJobFeedScreen`-ის `markInterested`, `ProviderJobDetailScreen`-ის `confirmInterest`) `id: string`-ის ნაცვლად მთელ `job: FeedJob`-ს იღებს ახლა, რომ `customerId`-მდე მიწვდეს.
      3. **Job-ის სტატუსის ცვლილება** — ოთხივე გადასვლაზე ცალკე, არსებულ call site-ებში ჩამატებული (არა ცენტრალიზებულად `jobService.updateJobStatus`-ში, რადგან იქ job-ის სათაური/counterparty-ის id ხელთ არ არის): `CustomerJobDetailScreen`-ის `confirmSelection` (→ Provider, "შენ აგირჩიეს"), `submitProblem` (→ Provider, "მომხმარებელმა პრობლემა აღნიშნა"), `openRating`-ის `onRate` (→ Provider, "სამუშაო დასრულებულად დადასტურდა"); `ProviderJobDetailScreen`-ის `markWorkDone` (→ Customer, "სამუშაო დასრულდა?"). ყველა გამოძახება `isUuid()`-ით არის დაცული (mock demo provider/customer id-ები არასდროს ცდილობენ არარსებულ `auth.users`-ზე FK-ს).
    - **`NotificationsScreen`** — mock `NOTIFICATIONS.filter(role)` საწყისად, `notificationService.listMine(uid)`-ის **მხოლოდ არა-ცარიელ** პასუხზე ჩანაცვლებით (#56-ის იგივე fallback-chain), `useFocusEffect`-ით (Bottom Tab-იდან ღია root-stack ეკრანი, მაგრამ ისევე შეიძლება ხელახლა გაიხსნას). `markRead`/`markAllRead` ლოკალურადაც და (real id-ებზე) Supabase-შიც წერენ.
    - **Home-ების ბელის წითელი წერტილი** (`CustomerHomeScreen`/`ProviderHomeScreen`) — mock `MOCK_UNREAD_COUNT` საწყისად, `subscribeToUnreadCount`-ის ცოცხალი მნიშვნელობით გადაფარვით (#68-ის tab-badge-ის იგივე მექანიზმი).
    - **`ProviderJobDetailScreen`-ის `handleChat`/`ProviderHomeScreen`-ის/`ProviderJobFeedScreen`-ის Job-ბარათის "ჩატი" ღილაკები კვლავ `chatId: 'c1'` მოცემულ mock-ს იყენებენ** — ეს ცნობილი, დარჩენილი შეზღუდვაა (FeedJob-ს Customer-ის `name`/`initials`/`color` ველები არ აქვს, მხოლოდ `customer: string` სახელი) — ცალკე, ამ ეტაპის ფარგლებს სცდება.
    - SQL:
      ```sql
      create table public.notifications (
        id uuid primary key default gen_random_uuid(),
        user_id uuid not null references auth.users(id),
        title text not null,
        body text not null,
        icon_emoji text not null,
        icon_bg text not null,
        target jsonb,
        read boolean not null default false,
        created_at timestamptz not null default now()
      );

      alter table public.notifications enable row level security;

      create policy "Owner can read own notifications"
        on public.notifications for select
        using (auth.uid() = user_id);

      create policy "Owner can update own notifications"
        on public.notifications for update
        using (auth.uid() = user_id);

      -- client-side ინსერტია (არა trigger/service role) — ნებისმიერ
      -- ავტორიზებულ მომხმარებელს სჭირდება შეეძლოს სხვისთვის შეტყობინების
      -- დაწერა (მაგ. Provider წერს Customer-ს ახალ დაინტერესებაზე).
      create policy "Any authenticated user can insert notifications"
        on public.notifications for insert
        with check (auth.role() = 'authenticated');

      alter publication supabase_realtime add table public.notifications;
      ```
71. **საბოლოო "fake-გასუფთავების" ეტაპი — ყველა mock domain-მონაცემი და mock-fallback კოდი წაშლილია.** მომხმარებლის პირდაპირი მოთხოვნა: "რაც არაა რეალური ის წავშალოთ საერთოდ აპიდან და გავასუფთავოთ კოდიც". #68-#70-ის (ჩატის სია/ფოტოები, job-ის რეალური ბარათები, შეტყობინებები) დასრულების შემდეგ ეს იყო ბოლო ნაბიჯი — ყველა დარჩენილი mock/real fallback chain (`route.params.job ?? jobService.getXxx() ?? mockArray[i]`, `getUnreadCount()`, mock provider დირექტორია, ჰარდქოდილი სტატისტიკები) ან რეალურ fetch-ზე გადავიდა, ან სრულად ამოღებულია.
    - **წაშლილია მთლიანად:** `src/data/mockHomeData.ts`, `mockChats.ts`, `mockReviews.ts`, `mockNotifications.ts` — 4 ფაილივე. `src/data/`-ში მხოლოდ ტაქსონომია (categories/districts/georgiaRegions/specialties/experience) დარჩა.
    - **სერვისებიდან მოცილებულია ყველა sync mock მეთოდი:** `jobService` (`listCustomerJobs`/`getCustomerJobById`/`listProviderFeed`/`getOpenProviderFeed`/`getFeedJobById`/`listMyActiveJobs`/`listMyDoneJobs`, პლუს `CURRENT_PROVIDER_ID` mock-constant), `quoteService` (`getQuotesForJob`), `chatService` (`listChats`/`getChatById`/`getMessages`), `reviewService` (`getReviewsForProvider`/`getCompletedJobs`/`getSubmittedRating`), `userService` (`listProviders`/`getProviderById`). ამის სანაცვლოდ **4 ახალი real-only single-fetch მეთოდი** დაემატა, სადაც აქამდე mock fallback-ს ეყრდნობოდა UI: `jobService.getJobPostById(id)`/`getFeedJobPostById(id)` (route param-ში job-ის არარსებობისას — notification deep-link), `reviewService.getReviewByJobId(jobId)` (Customer-ის "შენი შეფასება" სექცია + Provider-ის "completed" mode-ის მიღებული შეფასების ბანერი — ორივეს იზიარებს), `userService.getRealProviderById(id)` (ერთი Provider-ის პირდაპირი წაკითხვა, `listRealProviders()`-ის filter-ის ნაცვლად).
    - **`CustomerJobDetailScreen`/`ProviderJobDetailScreen` job-ის synchronous mock-fallback-derivation (`useMemo`) ჩანაცვლდა async real-fetch-ით** — ორივეს დაემატა `EMPTY_JOB`/`EMPTY_PROVIDER`-ის მსგავსი placeholder-obj + `loading` state + ერთი early-return loading-skeleton, ყველა hook-ის შემდეგ (React hooks rules-ის დაცვით — hook-ები `job.id`-ს placeholder ცარიელ სტრიქონზე უსაფრთხოდ მუშაობენ, სანამ რეალური fetch არ დასრულდება). `ViewProviderProfileScreen`-საც იგივე pattern დაემატა (`EMPTY_PROVIDER`).
    - **4 Provider-დირექტორიის ეკრანი** (`CustomerHomeScreen`, `CustomerCategoryScreen`, `SavedProvidersScreen`) — "mock + real merge" (#60-ის თავდაპირველი გადაწყვეტილება, `[...prev, ...real.filter(...)]`) ჩანაცვლდა სუფთა `listRealProviders()`-ით, საწყისი `[]`-ით.
    - **ჩატის mock/real "isReal" branching მთლიანად მოცილებულია `ChatConversationScreen`-იდან** — შესაძლებელი გახდა მას შემდეგ, რაც ყველა navigation call site რეალურ Supabase UUID-ს გადასცემს `chatId`-ად (იხ. ქვემოთ). წაშლილია: `isUuid`-ზე დამოკიდებული `isReal` flag, mock `getMessages(chatId)` საწყისი მნიშვნელობა (ახლა `[]`), ყველა `setTimeout`-ზე დაფუძნებული fake "sent" სიმულაცია. **mock chat-ის job-ბანერი (`chatEntry?.jobTitle`, `CategoryIcon`/`StatusPill`/"დეტ. ნახვა" ლინკი) მთლიანად ამოღებულია** — რეალურ `conversations`-ს (#68) job-თან კავშირი არ აქვს, ეს ბანერი მხოლოდ mock demo chat-ებზე ჩანდა. ფასის სტატუსის ტექსტი (`offerStatusText`, რეალურ `messages`-ზეა აგებული, #45) კი **დარჩა**, ახლა დამოუკიდებელი, მსუბუქი ბანერის სახით (job-ბანერისგან განცალკევებული).
    - **Online/offline პრეზენსი (`chatEntry?.online`, "ბოლოს ნახული: გუშინ") მთლიანად ამოღებულია** — ეს არასდროს ყოფილა რეალური მონაცემი (route param-შიც არ არსებობს), მხოლოდ mock-ის ჰარდქოდილი fallback ტექსტი იყო.
    - **3 ჩატის ღილაკის მუდმივი mock-id (`chatId: 'c1'`, `ProviderJobDetailScreen`/`ProviderHomeScreen`/`ProviderJobFeedScreen`-ის "ჩატი") რეალურად გასწორდა** — ეს შესაძლებელი გახდა `FeedJob`-ს ახალი `customerId?: string` ველის დამატებით (#70-ში `job_responses`-ის notification-ისთვის უკვე დამატებული იყო) — ახლა იგივე ველი ჩატის გახსნასაც ემსახურება. ანალოგიურად, `CustomerJobsScreen`-ის "ჩატი" ღილაკს ჰქონდა `chatId: 'p1'` — ახსნილია ახალი `CustomerJob.providerId?: string`-ით (job_posts.provider_id-დან, `provider`-ის (სახელი) გვერდით).
    - **ჰარდქოდილი ფეიკ სტატისტიკები/ბეჯები ამოღებულია ან რეალურით ჩანაცვლდა:**
      - `ProviderHomeScreen`-ის "ამ თვის სტატისტიკა" ბარათი (`14 სამ. / 2,840₾ შემოს. / 4.9★ შეფ.`) — "შემოს." (შემოსავალი) **მთლიანად ამოღებულია** (აპში ფასის აგრეგაციის მექანიზმი საერთოდ არ არსებობს, #35), დანარჩენი ორი რეალურ `provider_stats`-ზეა (`userService.getRealProviderById`). ბარათის ლეიბლიც "სტატისტიკა"-დ შეიცვალა ("ამ თვის" მოცილებულია — მონაცემი all-time არის, არა თვის-სპეციფიკური).
      - `ProviderProfileScreen`-ის header-ის `4.9 · 127 შეფ. · 312 სამ.` + სამივე `STATS` ბლოკი — რეალურ `stats`-ზეა (`userService.getRealProviderById`), `isNewProvider`-ის (#42) იგივე "ახალი ოსტატი" fallback-ით 0 შეფასებაზე. **ასევე მოცილებულია own-profile header-ის უპირობო `<VerifiedBadge/>`** (ყოველთვის ჩანდა, მიუხედავად `verified`-ის რეალური მნიშვნელობისა — დანარჩენ ყველა ეკრანზე ეს ბეჯი სწორად `p.verified &&`-ზეა გატანილი, real providers-ზე `verified` ყოველთვის `false` არის, ამიტომ იქ თავად ვერასდროს ჩანდა).
      - `CustomerProfileScreen`-ის "ჩემი მოთხოვნები" badge (`3`, ჰარდქოდილი) — რეალურ `jobService.listMyJobPosts(uid).length`-ზეა. "შეტყობინებები" badge-იც (ორივე Profile-ს) `notificationService.subscribeToUnreadCount`-ზეა, `getUnreadCount()`-ის (mock) ნაცვლად.
      - `ProviderJobDetailScreen`-ის "მომხმარებელი" სექციის `4.7 · 8 განცხ.` (Customer-ის ფეიკ რეიტინგი/post-count) — **მთლიანად ამოღებულია**, აპში Customer-ის რეიტინგის კონცეფცია საერთოდ არ არსებობს.
      - `ProviderJobDetailScreen`-ის "completed" mode-ის მიღებული შეფასების ბანერი (`receivedRating`, ადრე ყოველთვის ჰარდქოდილი "5.0 ⭐ ძალიან კარგი სამუშაო, მადლობა!") — რეალურ `reviewService.getReviewByJobId`-ზეა.
      - `CustomerJobDetailScreen`-ის `job.id === 'j1'`-ზე დამოკიდებული "🔥 გადაუდებელი" badge და `job.id !== 'j3'`-ზე დამოკიდებული 2-placeholder-ფოტოს fallback (`PHOTO_COLORS`) — ორივე მთლიანად ამოღებულია (mock demo id-ებზე მიბმული, რეალურ არცერთ job-ს არასდროს არ ჰქონდა შესაბამისობა).
    - **Tab-bar-ის badge-ები (`CustomerTabs`/`ProviderTabs`) ადრეც უკვე რეალური იყო (#68) — უცვლელი.**
    - **`NotificationsScreen`-ის mock `NOTIFICATIONS.filter(role)` fallback ამოღებულია** — ახლა ყოველთვის `notificationService.listMine(uid)`-ზეა, ცარიელი საწყისით.
    - **წაშლილია გამოუყენებელი domain ტიპებიც** (`MyJobRow`, `CompletedJob` — მხოლოდ წაშლილ mock-მასივებს ემსახურებოდნენ).
    - **Retry-ღილაკის ("ხელახლა", failed შეტყობინებაზე) ქცევა შეგნებულად დარჩა გამარტივებული** — ვიზუალურად "sending→sent" ცვლის state-ს, რეალურ ხელახლა-გაგზავნას (Supabase-ზე) არ ასრულებს ამ ეტაპზე; ეს UI-level მარტივი retry ფეიკ *მონაცემი* არ არის (არაფერს არ იგონებს), მხოლოდ არასრული ქცევაა — ცალკე, მომავალი გაუმჯობესებაა.
    - **Firebase-ის მოძველებული `docs/firebase-setup.md` საცნობარო დოკუმენტი (#52-ის შენიშვნით) არ შეხებია** — ისტორიული საცნობარო ფაილია, არა კოდი.
    - **SQL არ დასჭირდა** — ეს მთლიანად frontend/client-side რეფაქტორინგია, არცერთი ცხრილის სქემა არ შეცვლილა.

**ამით #52-დან დაწყებული Supabase-მიგრაციის ყველა ეტაპი — დაგეგმილი 6 ცხრილი (#52-#58), Realtime (#59), საჯარო დირექტორია (#60), Storage (#61-#62), და ყველა შემდგომი დახვეწა (#63-#67) — დასრულებულია.** ცნობილი, განზრახ დარჩენილი შეზღუდვები: Provider-ის საკუთარი "ჩემი სამუშაოები" ტაბი (`ProviderMyJobsScreen`, #30/#47-ის mock-კუნძული) და `conversations`-ის ცალკე ცხრილის არარსებობა (#57-ის განზრახული გამარტივება).

- ფერები: `src/theme/colors.ts` (light bg, ერთი აქცენტი, სემანტიკური success/warning/danger)
- Radius/spacing/typography ტოკენები: `src/theme/index.ts`
- Card radius: 12-16px (`radius.lg` = 16)
- Loading/empty/error/success state-ების ვიზუალური ენა: `docs/design-reference/app-states.md`-ის მიხედვით (skeleton-ები, არა spinner-ები; inline loading ღილაკებში)

## ნავიგაციის არქიტექტურა

Root არის ერთი `native-stack` (`src/navigation/RootNavigator.tsx`), ტიპიზირებული `RootStackParamList`-ით (`src/navigation/types.ts`). Onboarding-იდან Home-ზე გადასვლისას `navigation.reset()` გამოიყენება (არა `navigate`/`replace`) — მომხმარებელს არ შეუძლია უკან დაბრუნება რეგისტრაციაში ავტორიზაციის შემდეგ.

**Bottom Tabs** (`CustomerTabs`/`ProviderTabs`, `src/navigation/CustomerTabs.tsx` და `ProviderTabs.tsx`) — თითო ცალკე Tab Navigator როლის მიხედვით (არა ერთი საერთო + role param, იმავე პრინციპით რითიც CustomerHome/ProviderHome ცალკეა). ორივე ჩალაგებულია RootStack-ის **იგივე** `"CustomerHome"`/`"ProviderHome"` route-ების ქვეშ — ეს route-ის სახელები არ შეცვლილა განზრახ, რომ ყველა არსებული `navigation.reset({routes:[{name:'CustomerHome'}]})` გამოძახება უცვლელად იმუშაოს. თითო ტაბ-ნავიგატორს 3 ტაბი აქვს: **Home** (CustomerHomeScreen/ProviderHomeScreen), **Chats** (`ChatsListScreen`, role prop-ით გადაცემული children render prop-ის საშუალებით — ერთი საერთო კომპონენტი ორივე როლისთვის), **Profile** (`ProviderProfileScreen`/`CustomerProfileScreen` — E1/E2, `role`-ის მიხედვით ცალკე კომპონენტი თითო ტაბ-ნავიგატორში).

Tab-ის შიგნით მდებარე ეკრანების (`CustomerHomeScreen` და ა.შ.) navigation prop ტიპია `CompositeScreenProps<BottomTabScreenProps<...>, NativeStackScreenProps<RootStackParamList>>` — საჭიროა root-stack route-ებზე (`PostJob`, `ChatConversation` და ა.შ.) ნავიგაციისთვის ტაბის შიგნიდან. **Parent stack-ის action-ები** (მაგ. `reset`) ტაბის შიგნიდან უნდა გამოიძახოს `navigation.getParent()?.reset(...)`-ით, არა პირდაპირ `navigation.reset(...)`-ით — TypeScript-ის composite ტიპი `reset`-ს ტაბ-navigator-ზე resolve-ავს.

`ChatConversation`, `ProviderJobDetail`, `CustomerJobDetail`, `PostJob` — root-stack screen-ებია (ტაბების გარეთ, ტაბ-ბარს ფარავენ push-ისას), არა tab-ის შიგნით ჩალაგებული.

## პროექტის სტრუქტურა

```
src/
  components/   # საერთო UI კომპონენტები (Button, TextField, Chip, Avatar, StatusPill,
                # BottomSheet, InlineBanner, RegionAreaAccordion, SpecialtyPickerField,
                # ExperiencePickerField, MediaUploadGrid, MediaPreviewModal,
                # CalendarPicker, DatePickerField, ProviderFeedJobCard,
                # AddressAutocompleteField, ...)
  data/         # მხოლოდ ტაქსონომია/ლექსიკონი (#71-ის მიხედვით, mock
                # domain-მონაცემები მთლიანად წაშლილია) — categories,
                # districts, georgiaRegions, specialties, experience.
  navigation/   # RootNavigator, CustomerTabs, ProviderTabs + ტიპები
  screens/      # თითო ეკრანი — თითო ფაილი
  services/     # data access ფენა (domain models refactor) — authService,
                # userService, jobService, quoteService, chatService,
                # reviewService, notificationService, storageService,
                # supabaseClient. ყველა რეალურად Supabase-ზეა (#71-ის
                # მიხედვით mock-fallback-methods მთლიანად წაშლილია) —
                # authService-ის გარდა სინქრონული (ეკრანების render-ის
                # დროს პირდაპირი წაკითხვის სტრუქტურის დასაცავად).
  state/        # Global state (React Context) — CustomerProfileContext,
                # FavoriteProvidersContext, ProviderProfileContext, JobStatusContext
                # (პირველი ორი ახლა userService-ის თხელი რეაქტიული shell-ია)
  theme/        # ფერები, radius, spacing, typography ტოკენები
  types/        # domain model ტიპები (domain models refactor) — user, provider,
                # job, quote, chat, review, notification. StatusPill.tsx/
                # navigation/types.ts ამ ფაილებიდან რეექსპორტავენ
                # JobStatus/Role-ს, ბექვარდ თავსებადობისთვის.
  utils/        # საერთო წმინდა ფუნქციები (providerRank.ts — რეიტინგის
                # წონიანი ფორმულა და "ახალი ოსტატის" დადგენა)
docs/
  design-reference/   # საწყისი სპეციფიკაციები, გადატანილი Downloads-იდან მუდმივი წვდომისთვის
  firebase-setup.md   # ინსტრუქცია რეალური Firebase პროექტის დასაკავშირებლად
```

## აშენებული ეკრანები (მდგომარეობა ამ დოკუმენტის ბოლო განახლებისას)

ზიპის (Figma Make React web პროტოტიპის) **ყველა რეალურად გამოყენებადი ეკრანი უკვე აშენებულია.** დეტალურად:

**A — Onboarding:** Welcome, RoleSelect, Register (customer-ისთვის სახელი/გვარი გაყოფილი, იხ. გადაწყვეტილება #8-ის თავზე), Login, ForgotPassword, GoogleComplete, CustomerSetup, ProviderSetup (specialty dropdown + Georgia-ის რეგიონების area picker) — **დასრულებული**
**B — Provider:** ProviderHome (B1), ProviderJobDetail (B2, browse/selected/**completed** mode) — **დასრულებული**
**C — Customer:** CustomerHome/Browse (C1, 2×2 სერვისების ბადე), PostJob (C2, ფოტო-ლიმიტი 3-ზე override + ახალი მისამართის ველი), CustomerJobDetail (C3+C4, სრული completion/problem/rating flow) — **დასრულებული**
**D — Chat:** ChatsList (D1), ChatConversation (D2), ფასის შეთავაზების ბარათი (D3) — **დასრულებული**, იხ. არქიტექტურული გადაწყვეტილება #2
**E — პროფილები:** ProviderProfile (E1), CustomerProfile (E2) — **დასრულებული**, ორივე მენიუს პუნქტი მიბმულია რეალურ ეკრანზე (ქვემოთ). "გასვლა" BottomSheet-ით (`navigation.getParent()?.reset()` Welcome-ზე).
**პროფილის ქვე-ეკრანები (ახალი, ზიპიდან გადმოტანილი):** NotificationsScreen + NotificationSettingsScreen, ProfileSettings (პაროლის შეცვლა), CustomerEditProfile, ProviderEditProfile, ProviderServiceAreas, ProviderCompletedJobs, ProviderReviews, ViewProviderProfile (Customer-ის მხრიდან ოსტატის საჯარო პროფილი) — **ყველა დასრულებული**. `CustomerJobsScreen` და `ProviderMyJobsScreen` (ზიპშივე ორივე orphan იყო) ორივე ახლა root-stack push-ის ნაცვლად საკუთარი Bottom Tab-ია (`MyJobsTab`, Customer-ისთვისაც და Provider-ისთვისაც — #36, #40).
**RatingScreen** (product-spec.md პუნქტი #14) — **დასრულებული**, ინტეგრირებულია `CustomerJobDetailScreen`-ის completion flow-ში (იხ. გადაწყვეტილება #6).
**Notifications** — **დასრულებული**, `NotificationsScreen`/`NotificationSettingsScreen`, bell ღილაკები ყველგან რეალურად მიბმულია.
**`CustomerCategoriesScreen`/`CustomerCategoryScreen`** — ახალი, ზიპში არ არსებობდა ამ ფორმით (იხ. გადაწყვეტილება #10-11).
**`RegionAreaPickerScreen`** — ახალი, ზიპში საერთოდ არ არსებობდა (იხ. გადაწყვეტილება #9).
**შეგნებულად არ აშენებულა:** ზიპის `ErrorState`/`OfflineState` გენერიკული კომპონენტები — არცერთ ეკრანზე რეალურად არ იყო გამოყენებული ზიპშივე (მკვდარი კოდი). "დახმარება" მენიუს პუნქტი (Customer/Provider Profile) — ზიპშივე `screen: null` იყო, დანიშნულების ეკრანი არასდროს არსებობდა.

## ცნობილი ტექნიკური თავისებურებები ამ მანქანაზე

- **JDK 25 (Android Studio-ს ჩაშენებული) ვერ ვარგისობს Android native build-ისთვის** — გამოიყენე Eclipse Temurin JDK 17 (`C:\Program Files\Eclipse Adoptium\jdk-17.0.20.101-hotspot`)
- **ძველი Metro პროცესი port 8081-ზე** შეიძლება მალავდეს ახალ ფაილებს "unresolvable module" შეცდომით — შეამოწმე `Get-NetTCPConnection -LocalPort 8081` პროცესის დაწყების დროით
- **ეკრანის კომპონენტში ორი ცალკე top-level `return (<SafeAreaView>...)` (მაგ. `if (loading) return (...)` სულ სხვა JSX ხით, მერე მთავარი `return (...)`) იწვევს Fabric-ის რეალურ crash-ს Android-ზე** — `java.lang.IllegalStateException: addViewAt: failed to insert view` / "The specified child already has a parent". ხდება მაშინ, როცა fetch (loading→loaded) თითქმის მყისიერად სრულდება screen-ის navigation-transition-ის დროს — Fabric ვერ ასწრებს ძველი ხის unmount-ს ახლის mount-მდე. **გამოსავალი: ერთი მუდმივი JSX ხე** — SafeAreaView/header ერთხელ დაწერილი, loading-ის მიხედვით მხოლოდ შიდა content იცვლება (`{loading ? <Skeleton/> : <ScrollView>...</ScrollView>}`), არა ორი ცალკე `return`. ეს პატერნი დაფიქსირდა და გასწორდა `ViewProviderProfileScreen`/`CustomerJobDetailScreen`/`ProviderJobDetailScreen`-ში (#71-ის real-fetch loading-guard-ების დამატებისას წარმოქმნილი ბაგი) — ახალი async-loading ეკრანის დამატებისას ეს პატერნი გასაზიარებელია.
